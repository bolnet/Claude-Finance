---
phase: 05-demo-command-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - tests/test_demo_command.py
autonomous: false
requirements:
  - DEMO-01
  - DEMO-02
  - DEMO-03
  - DEMO-04

must_haves:
  truths:
    - "Automated tests confirm demo.md structure is correct and complete"
    - "Human verifies /demo is discoverable in Claude Code autocomplete"
    - "All 11 tool references are present in the walkthrough"
    - "Welcome and completion sections are properly positioned"
  artifacts:
    - path: "tests/test_demo_command.py"
      provides: "Structural validation tests for demo command"
      min_lines: 30
  key_links:
    - from: "tests/test_demo_command.py"
      to: ".claude/commands/demo.md"
      via: "file read and content assertions"
      pattern: "demo.md"
---

<objective>
Verify the /demo command is structurally complete, discoverable, and ready for use.

Purpose: Automated tests catch structural regressions; human verification confirms Claude Code integration works.
Output: `tests/test_demo_command.py` test suite, human sign-off
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-demo-command-flow/05-01-SUMMARY.md

<interfaces>
<!-- Test pattern from Phase 4 persona command tests -->
From tests/test_persona_commands.py:
- Read command file as text
- Split on "---" to extract frontmatter
- Assert required fields present
- Assert content sections exist via substring/regex checks

<!-- Expected demo.md structure from Plan 05-01 -->
Required in demo.md:
- Frontmatter: description, allowed-tools (13 MCP tool names), model
- Body: Welcome section, 11 numbered steps, persona showcase, completion summary
- All 13 mcp__finance__* tool names in allowed-tools
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create structural validation tests for demo command</name>
  <files>tests/test_demo_command.py</files>
  <behavior>
    - Test 1: demo.md file exists at .claude/commands/demo.md
    - Test 2: Frontmatter contains required fields (description, allowed-tools, model)
    - Test 3: allowed-tools lists all 13 MCP tool function names (ping, validate_environment, analyze_stock, get_returns, get_volatility, get_risk_metrics, compare_tickers, correlation_map, ingest_csv, liquidity_predictor, predict_liquidity, investor_classifier, classify_investor)
    - Test 4: Body contains a welcome/introduction section (check for keywords like "Finance AI Skill", "walkthrough", "demo")
    - Test 5: Body contains all 11 step markers (Step 1 through Step 11) or equivalent numbered sections
    - Test 6: Body contains completion/summary section (check for keywords like "complete", "summary", "demonstrated")
    - Test 7: Body references both personas (/finance-analyst, /finance-pm)
    - Test 8: Body contains the mandatory disclaimer text
    - Test 9: No argument-hint in frontmatter (demo takes no arguments)
  </behavior>
  <action>
Create `tests/test_demo_command.py` following the same file-read-and-parse pattern used in `tests/test_persona_commands.py`:

1. Read `.claude/commands/demo.md` as text in a fixture
2. Split on `---` to extract frontmatter and body
3. Write one test function per behavior above
4. Use `pathlib.Path` for file existence check
5. Use simple string matching (no YAML parser needed — consistent with Phase 4 pattern)

Run tests with: `.venv/bin/python3 -m pytest tests/test_demo_command.py -v`

All 9 tests must pass.
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_demo_command.py -v</automated>
  </verify>
  <done>
    - 9 tests pass covering frontmatter, tool coverage, step count, welcome, summary, personas, disclaimer
    - Tests follow project convention (pathlib, string parsing, no external YAML dependency)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of /demo command discoverability</name>
  <action>Human verifies /demo appears in Claude Code autocomplete and walkthrough structure is well-organized.</action>
  <what-built>
    The /demo slash command is registered at .claude/commands/demo.md with a full walkthrough script covering all 11 MCP tools, pause-and-explain instructions, welcome message, and completion summary. SKILL.md has been updated with a demo intent. Automated tests confirm structural completeness.
  </what-built>
  <how-to-verify>
    1. In Claude Code, type `/` and verify `demo` appears in the autocomplete list
    2. Read `.claude/commands/demo.md` and confirm:
       - Welcome section clearly explains what the Finance AI Skill does
       - 11 steps are clearly defined with specific tool names and parameters
       - Each step has explanation instructions
       - Completion summary lists all tools by category
    3. Optionally run `/demo` to see the first few steps execute (you can cancel early — full end-to-end demo will be verified in Phase 8)
  </how-to-verify>
  <verify>Human confirms /demo appears in autocomplete and walkthrough structure is correct</verify>
  <done>Human approves demo command discoverability and structural completeness</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `pytest tests/test_demo_command.py -v` — all 9 tests green
2. Full test suite still passes: `.venv/bin/python3 -m pytest -x` (no regressions)
3. Human confirms /demo is discoverable in Claude Code
</verification>

<success_criteria>
- All 9 structural tests pass
- No regressions in existing test suite
- Human confirms /demo appears in Claude Code autocomplete
- Human approves walkthrough structure
</success_criteria>

<output>
After completion, create `.planning/phases/05-demo-command-flow/05-02-SUMMARY.md`
</output>

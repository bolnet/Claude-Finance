---
phase: 07-ml-workflow-demos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - demo/sample_portfolio.csv
  - tests/test_sample_csv.py
autonomous: true
requirements:
  - MLWF-01

must_haves:
  truths:
    - "demo/sample_portfolio.csv exists and is loadable by pandas"
    - "CSV contains columns for both liquidity and investor models"
    - "CSV has enough rows for train/test split (minimum 50)"
  artifacts:
    - path: "demo/sample_portfolio.csv"
      provides: "Bundled sample data for ML demo steps"
      contains: "credit_score,debt_ratio,region,liquidity_risk,age,income,risk_tolerance,product_preference,segment"
    - path: "tests/test_sample_csv.py"
      provides: "Schema validation tests for sample CSV"
  key_links:
    - from: "demo/sample_portfolio.csv"
      to: "src/finance_mcp/tools/csv_ingest.py"
      via: "ingest_csv(csv_path)"
      pattern: "pd.read_csv"
    - from: "demo/sample_portfolio.csv"
      to: "src/finance_mcp/tools/liquidity_model.py"
      via: "liquidity_predictor(csv_path)"
      pattern: "target_column.*liquidity_risk"
    - from: "demo/sample_portfolio.csv"
      to: "src/finance_mcp/tools/investor_model.py"
      via: "investor_classifier(csv_path)"
      pattern: "target_column.*segment"
---

<objective>
Create a bundled sample CSV file with realistic synthetic data that satisfies the column schemas of all three ML tools (ingest_csv, liquidity_predictor, investor_classifier).

Purpose: Steps 9-11 of demo.md reference demo/sample_portfolio.csv but it does not exist yet. Without it, those steps skip with a warning message.
Output: demo/sample_portfolio.csv with 100 rows of synthetic portfolio data, plus schema validation tests.
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/finance_mcp/tools/csv_ingest.py
@src/finance_mcp/tools/liquidity_model.py
@src/finance_mcp/tools/investor_model.py

<interfaces>
<!-- Column schemas extracted from tool source code -->

liquidity_model.py predict_liquidity expects:
  - credit_score: float (e.g. 650-800)
  - debt_ratio: float (0.0-1.0)
  - region: str ("Northeast", "South", "Midwest", "West")
  - liquidity_risk: float target column (0.0-1.0)

investor_model.py classify_investor expects:
  - age: float (e.g. 25-70)
  - income: float (e.g. 30000-250000)
  - risk_tolerance: float (0.0-1.0)
  - product_preference: str ("stocks", "bonds", "mixed", "equities")
  - segment: str target column ("conservative", "moderate", "aggressive")

csv_ingest.py ingest_csv:
  - Accepts any CSV, auto-detects numeric vs categorical
  - Runs IQR outlier cleaning on numeric columns
  - Generates histogram charts for up to 4 numeric columns
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create sample CSV and schema tests</name>
  <files>demo/sample_portfolio.csv, tests/test_sample_csv.py</files>
  <behavior>
    - Test 1: demo/sample_portfolio.csv exists and loads via pd.read_csv without error
    - Test 2: CSV has exactly these columns: credit_score, debt_ratio, region, liquidity_risk, age, income, risk_tolerance, product_preference, segment
    - Test 3: CSV has at least 50 rows (enough for 80/20 train/test split)
    - Test 4: credit_score values are numeric between 300-850
    - Test 5: debt_ratio values are numeric between 0.0-1.0
    - Test 6: region values are strings from {"Northeast", "South", "Midwest", "West"}
    - Test 7: liquidity_risk values are numeric between 0.0-1.0
    - Test 8: age values are numeric between 18-80
    - Test 9: income values are numeric between 20000-500000
    - Test 10: risk_tolerance values are numeric between 0.0-1.0
    - Test 11: product_preference values are strings from {"stocks", "bonds", "mixed", "equities"}
    - Test 12: segment values are strings from {"conservative", "moderate", "aggressive"}
    - Test 13: Each segment has at least 10 rows (enough for stratified split)
  </behavior>
  <action>
Write tests/test_sample_csv.py FIRST with all 13 tests above. Run tests — they MUST fail (RED).

Then create demo/sample_portfolio.csv with a Python script that generates 100 rows of synthetic data:
- Use numpy with seed=42 for reproducibility
- credit_score: np.random.randint(500, 820, 100) — realistic distribution
- debt_ratio: np.random.uniform(0.1, 0.8, 100).round(2)
- region: np.random.choice(["Northeast", "South", "Midwest", "West"], 100)
- liquidity_risk: derive from credit_score and debt_ratio with noise — lower credit + higher debt = higher risk. Formula: clip(1 - credit_score/1000 + debt_ratio * 0.3 + noise, 0, 1)
- age: np.random.randint(22, 68, 100)
- income: np.random.randint(35000, 200000, 100)
- risk_tolerance: np.random.uniform(0.1, 0.9, 100).round(2)
- product_preference: np.random.choice(["stocks", "bonds", "mixed", "equities"], 100)
- segment: derive from risk_tolerance — [0, 0.35) = "conservative", [0.35, 0.65) = "moderate", [0.65, 1.0] = "aggressive"

IMPORTANT: After generating, verify each segment has at least 10 rows. If not, adjust boundaries until balanced.

Write the CSV to demo/sample_portfolio.csv (create demo/ directory first). Then run tests — they MUST pass (GREEN).
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_sample_csv.py -v</automated>
  </verify>
  <done>demo/sample_portfolio.csv exists with 100 rows, 9 columns matching both ML tool schemas. All 13 schema tests pass.</done>
</task>

</tasks>

<verification>
- demo/sample_portfolio.csv exists and is valid CSV
- All schema validation tests pass
- CSV columns match liquidity_model and investor_model expectations
</verification>

<success_criteria>
- demo/sample_portfolio.csv committed to repo with 100 rows of realistic synthetic data
- tests/test_sample_csv.py passes with all 13 assertions
- CSV is loadable by all 3 ML tools without column errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-ml-workflow-demos/07-01-SUMMARY.md`
</output>

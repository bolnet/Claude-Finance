---
phase: 06-market-analysis-demos
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/demo.md
  - tests/test_market_demo.py
autonomous: true
requirements:
  - MRKT-01
  - MRKT-02
  - MRKT-03
  - MRKT-04
  - MRKT-05
  - MRKT-06

must_haves:
  truths:
    - "analyze_stock returns formatted output with chart path when called with AAPL and a 90-day date range"
    - "get_returns returns formatted output with daily and cumulative return values for AAPL"
    - "get_volatility returns formatted output with annualized volatility value for AAPL"
    - "get_risk_metrics returns formatted output with Sharpe ratio, max drawdown, and beta for AAPL"
    - "compare_tickers returns formatted output with chart path for AAPL vs MSFT"
    - "correlation_map returns formatted output with heatmap chart path for AAPL, MSFT, GOOGL"
    - "demo.md Steps 3-8 use start/end dates matching actual tool parameter schemas"
  artifacts:
    - path: "tests/test_market_demo.py"
      provides: "Live integration tests for all 6 market analysis tools"
      min_lines: 80
    - path: ".claude/commands/demo.md"
      provides: "Updated demo steps with correct tool parameter format"
      contains: "start:"
  key_links:
    - from: "tests/test_market_demo.py"
      to: "src/finance_mcp/tools/price_chart.py"
      via: "direct function import"
      pattern: "from finance_mcp.tools.price_chart import analyze_stock"
    - from: "tests/test_market_demo.py"
      to: "src/finance_mcp/tools/risk_metrics.py"
      via: "direct function import"
      pattern: "from finance_mcp.tools.risk_metrics import get_risk_metrics"
    - from: ".claude/commands/demo.md"
      to: "mcp__finance__analyze_stock"
      via: "MCP tool invocation instructions"
      pattern: "analyze_stock"
---

<objective>
Fix demo.md market analysis steps (3-8) to use correct tool parameter schemas (start/end dates instead of period strings), and create live integration tests that invoke all 6 market analysis tools with real ticker data to verify they produce correct formatted output with charts.

Purpose: The demo.md currently instructs Claude to pass `period: "90d"` but the MCP tool schemas only accept `start` and `end` ISO date strings. Fixing this eliminates ambiguity. The integration tests prove all 6 tools work end-to-end with real Yahoo Finance data before the human verification step.

Output: Updated demo.md with correct parameter format; test_market_demo.py with 6 live integration tests
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/commands/demo.md
@src/finance_mcp/tools/price_chart.py
@src/finance_mcp/tools/returns.py
@src/finance_mcp/tools/volatility.py
@src/finance_mcp/tools/risk_metrics.py
@src/finance_mcp/tools/comparison.py
@src/finance_mcp/tools/correlation.py

<interfaces>
<!-- Tool function signatures that tests will call directly -->

From src/finance_mcp/tools/price_chart.py:
```python
def analyze_stock(ticker: str, start: str, end: str = "") -> str:
```

From src/finance_mcp/tools/returns.py:
```python
def get_returns(ticker: str, start: str, end: str = "") -> str:
```

From src/finance_mcp/tools/volatility.py:
```python
def get_volatility(ticker: str, start: str, end: str = "") -> str:
```

From src/finance_mcp/tools/risk_metrics.py:
```python
def get_risk_metrics(ticker: str, start: str, end: str = "") -> str:
```

From src/finance_mcp/tools/comparison.py:
```python
def compare_tickers(tickers: str, start: str, end: str = "") -> str:
```

From src/finance_mcp/tools/correlation.py:
```python
def correlation_map(tickers: str, start: str, end: str = "") -> str:
```

Note: `tickers` is a comma-separated string (e.g., "AAPL,MSFT"), not a list.
The `start` param is ISO date string "YYYY-MM-DD". `end` defaults to today.
All tools return a formatted string with plain-English interpretation, data, chart paths, and disclaimer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix demo.md Steps 3-8 to use correct tool parameter schemas</name>
  <files>.claude/commands/demo.md</files>
  <action>
Update Steps 3-8 in demo.md to replace `period: "90d"` and `period: "1y"` with explicit `start:` date parameters that match the actual MCP tool function signatures.

For each step, replace the parameter instructions:
- Step 3 (analyze_stock): Change `period: "90d"` to `start:` set to 90 days before today's date in ISO format (compute dynamically using the pattern "Use a start date 90 days ago in YYYY-MM-DD format")
- Step 4 (get_returns): Same — replace `period: "90d"` with start date 90 days ago
- Step 5 (get_volatility): Same — replace `period: "90d"` with start date 90 days ago
- Step 6 (get_risk_metrics): Change `period: "1y"` to start date 1 year ago
- Step 7 (compare_tickers): Change `period: "90d"` to start date 90 days ago. Also change `tickers: ["AAPL", "MSFT"]` to `tickers: "AAPL,MSFT"` (comma-separated string, not list)
- Step 8 (correlation_map): Change `period: "90d"` to start date 90 days ago. Also change `tickers: ["AAPL", "MSFT", "GOOGL"]` to `tickers: "AAPL,MSFT,GOOGL"` (comma-separated string, not list)

For the start date, use a dynamic instruction pattern that tells Claude to compute the date at runtime. For example:
```
- ticker: "AAPL"
- start: [90 days before today, in YYYY-MM-DD format]
```

This is a prompt, not code — Claude will compute the date when executing the demo.

Do NOT change anything outside Steps 3-8. Do NOT change the explanation text, the allowed-tools frontmatter, or any other section. Preserve all existing content exactly.

Verify the existing structural tests still pass after the edit (the tests check for step sections and tool names, not parameter values, so they should pass unchanged).
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_demo_command.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>Steps 3-8 use start/end date parameters matching actual tool schemas; tickers params use comma-separated strings; all 9 existing structural tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create live integration tests for all 6 market analysis tools</name>
  <files>tests/test_market_demo.py</files>
  <behavior>
    - test_analyze_stock_live: analyze_stock("AAPL", start_90d_ago) returns string containing "Chart saved" and "AAPL"
    - test_get_returns_live: get_returns("AAPL", start_90d_ago) returns string containing "return" (case-insensitive) and "AAPL"
    - test_get_volatility_live: get_volatility("AAPL", start_90d_ago) returns string containing "volatility" (case-insensitive) and "AAPL"
    - test_get_risk_metrics_live: get_risk_metrics("AAPL", start_365d_ago) returns string containing "Sharpe" and "drawdown" and "beta" (case-insensitive)
    - test_compare_tickers_live: compare_tickers("AAPL,MSFT", start_90d_ago) returns string containing "Chart saved" or "chart"
    - test_correlation_map_live: correlation_map("AAPL,MSFT,GOOGL", start_90d_ago) returns string containing "correlation" (case-insensitive) or "heatmap"
  </behavior>
  <action>
Create tests/test_market_demo.py with 6 integration tests that call the actual tool functions with real tickers and real Yahoo Finance data.

Key implementation details:
- Mark all tests with `@pytest.mark.network` so they can be skipped in offline CI environments
- Use a module-level fixture to compute `start_90d` and `start_1y` dates as ISO strings using `datetime.date.today() - datetime.timedelta(days=N)`
- Import each tool function directly from its module (e.g., `from finance_mcp.tools.price_chart import analyze_stock`)
- Add `sys.path.insert(0, "src")` in a conftest or at module top if needed
- Each test calls the tool function, asserts it returns a non-empty string, and asserts the string contains expected keywords proving the tool produced real output (not an error)
- For chart-producing tools (analyze_stock, get_returns, get_volatility, compare_tickers, correlation_map), assert the output contains a file path ending in .png
- For get_risk_metrics, assert the output contains all three metric names: Sharpe, drawdown, beta
- Use `pytest.mark.xfail(reason="network")` as a fallback if Yahoo Finance is unreachable — but do NOT mock anything; these are live integration tests

Do NOT import or use any mocks. These tests must hit real Yahoo Finance data to prove the demo will work.
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_market_demo.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 integration tests pass with live Yahoo Finance data; each test proves its corresponding tool returns correct formatted output with expected content (chart paths, metric names, ticker references)</done>
</task>

</tasks>

<verification>
- All 9 existing demo structural tests pass (no regression from demo.md edits)
- All 6 new market demo integration tests pass with live data
- Full test suite runs without new failures
</verification>

<success_criteria>
- demo.md Steps 3-8 use start/end parameters matching actual MCP tool schemas
- 6 live integration tests prove each market analysis tool works end-to-end with real ticker data
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/06-market-analysis-demos/06-01-SUMMARY.md`
</output>

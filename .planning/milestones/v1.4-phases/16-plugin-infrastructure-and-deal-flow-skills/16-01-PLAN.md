---
phase: 16-plugin-infrastructure-and-deal-flow-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - finance-mcp-plugin/.claude-plugin/plugin.json
  - finance-mcp-plugin/hooks/hooks.json
  - finance-mcp-plugin/.mcp.json
  - tests/test_plugin_manifest.py
autonomous: true
requirements: [PLUG-01, PLUG-02, PLUG-03, PLUG-04]

must_haves:
  truths:
    - "plugin.json has correct bolnet/Claude-Finance URLs with no [owner] placeholder"
    - "hooks/hooks.json exists as an empty array"
    - ".mcp.json references the finance MCP server with correct command/args"
    - "Plugin directory has all required subdirectories: .claude-plugin/, commands/, skills/, hooks/"
  artifacts:
    - path: "finance-mcp-plugin/.claude-plugin/plugin.json"
      provides: "Plugin manifest with name, version, author, URLs"
      contains: "bolnet"
    - path: "finance-mcp-plugin/hooks/hooks.json"
      provides: "Empty hooks array per Anthropic pattern"
      contains: "[]"
    - path: "finance-mcp-plugin/.mcp.json"
      provides: "MCP server configuration for tool-powered skills"
      contains: "finance_mcp.server"
    - path: "tests/test_plugin_manifest.py"
      provides: "Validation tests for all plugin infrastructure"
      contains: "test_hooks_json"
  key_links:
    - from: "finance-mcp-plugin/.claude-plugin/plugin.json"
      to: "https://github.com/bolnet/Claude-Finance"
      via: "homepage and repository fields"
      pattern: "bolnet/Claude-Finance"
    - from: "finance-mcp-plugin/.mcp.json"
      to: "src/finance_mcp/server.py"
      via: "command + args configuration"
      pattern: "finance_mcp\\.server"
---

<objective>
Set up the complete Anthropic-pattern plugin infrastructure: fix plugin.json with real URLs, create hooks/hooks.json, verify .mcp.json wiring, and ensure directory structure is correct.

Purpose: The plugin must be installable via `claude plugin install` with no placeholder values or missing files.
Output: Valid plugin manifest, hooks file, MCP config, and expanded test suite proving everything works.
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@finance-mcp-plugin/.claude-plugin/plugin.json
@finance-mcp-plugin/.mcp.json
@tests/test_plugin_manifest.py
</context>

<interfaces>
<!-- Existing plugin.json structure (needs URL fix): -->
```json
{
  "name": "finance-mcp",
  "version": "1.0.0",
  "description": "Finance AI skill...",
  "author": { "name": "Finance MCP Project" },
  "homepage": "https://github.com/[owner]/machine_learning_skill",
  "repository": "https://github.com/[owner]/machine_learning_skill"
}
```

<!-- Existing .mcp.json (already correct, verify only): -->
```json
{
  "mcpServers": {
    "finance": {
      "command": "python",
      "args": ["-m", "finance_mcp.server"]
    }
  }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix plugin.json and create hooks/hooks.json</name>
  <files>finance-mcp-plugin/.claude-plugin/plugin.json, finance-mcp-plugin/hooks/hooks.json</files>
  <action>
1. Update `finance-mcp-plugin/.claude-plugin/plugin.json`:
   - Change `homepage` from `https://github.com/[owner]/machine_learning_skill` to `https://github.com/bolnet/Claude-Finance`
   - Change `repository` from `https://github.com/[owner]/machine_learning_skill` to `https://github.com/bolnet/Claude-Finance`
   - Update `version` to `"1.4.0"` (v1.4 milestone)
   - Update `description` to: `"Private Equity plugin for Claude Code — deal flow, portfolio monitoring, and ML-powered analytics for PE professionals. Powered by finance-mcp with 11 MCP tools."`
   - Add `"private-equity"`, `"deal-flow"`, `"due-diligence"` to keywords list
   - Keep all other fields as-is

2. Create `finance-mcp-plugin/hooks/hooks.json` with content: `[]` (empty JSON array — Anthropic pattern requires the file to exist even with no hooks)

3. Verify `.mcp.json` is already correct (it is — command: python, args: ["-m", "finance_mcp.server"]). No changes needed.
  </action>
  <verify>
    <automated>python3 -c "
import json
from pathlib import Path
p = Path('finance-mcp-plugin')
# Check plugin.json
pj = json.loads((p / '.claude-plugin/plugin.json').read_text())
assert 'bolnet' in pj['homepage'], f'homepage still has placeholder: {pj[\"homepage\"]}'
assert 'bolnet' in pj['repository'], f'repository still has placeholder: {pj[\"repository\"]}'
assert '[owner]' not in json.dumps(pj), 'Still has [owner] placeholder'
assert pj['version'] == '1.4.0'
# Check hooks
hj = json.loads((p / 'hooks/hooks.json').read_text())
assert hj == [], f'hooks.json should be empty array, got: {hj}'
# Check .mcp.json
mj = json.loads((p / '.mcp.json').read_text())
assert 'finance' in mj['mcpServers']
print('All plugin infrastructure checks passed')
"
    </automated>
  </verify>
  <done>plugin.json has bolnet/Claude-Finance URLs, version 1.4.0, PE-focused description and keywords. hooks/hooks.json exists as empty array. .mcp.json verified correct. No [owner] placeholders remain.</done>
</task>

<task type="auto">
  <name>Task 2: Expand plugin test suite for new infrastructure</name>
  <files>tests/test_plugin_manifest.py</files>
  <action>
Add the following test functions to `tests/test_plugin_manifest.py` (keep all existing tests):

1. `test_plugin_json_no_placeholders` — Load plugin.json as text, assert `[owner]` not in text. Ensures no placeholder strings remain.

2. `test_plugin_json_correct_urls` — Load plugin.json, verify `homepage` and `repository` both contain `bolnet/Claude-Finance`.

3. `test_plugin_json_version_format` — Load plugin.json, verify `version` matches semver pattern (X.Y.Z).

4. `test_hooks_json_exists` — Assert `finance-mcp-plugin/hooks/hooks.json` exists.

5. `test_hooks_json_empty_array` — Load hooks.json, parse as JSON, assert it equals `[]`.

6. `test_plugin_directory_structure` — Assert all 4 required subdirectories exist: `.claude-plugin/`, `commands/`, `skills/`, `hooks/`.

7. `test_mcp_json_has_finance_server` — Load .mcp.json, assert `mcpServers.finance` exists with `command` and `args` keys.

Use the same `PROJECT_ROOT` and `PLUGIN_DIR` constants from the existing file. Follow the same docstring style.
  </action>
  <verify>
    <automated>cd /Users/aarjay/Documents/machine_learning_skill && python3 -m pytest tests/test_plugin_manifest.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All existing + new plugin manifest tests pass. Test suite covers: no placeholders, correct URLs, version format, hooks.json existence and content, directory structure, MCP server config.</done>
</task>

</tasks>

<verification>
All plugin infrastructure requirements verified:
- PLUG-01: plugin.json has correct fields and URLs
- PLUG-02: hooks/hooks.json exists as empty array
- PLUG-03: .mcp.json references finance MCP server
- PLUG-04: Directory structure has .claude-plugin/, commands/, skills/, hooks/
</verification>

<success_criteria>
1. `python3 -m pytest tests/test_plugin_manifest.py -v` passes all tests (existing + new)
2. No `[owner]` placeholder in any file under finance-mcp-plugin/
3. hooks/hooks.json exists and contains `[]`
4. Directory has .claude-plugin/, commands/, skills/, hooks/ subdirectories
</success_criteria>

<output>
After completion, create `.planning/phases/16-plugin-infrastructure-and-deal-flow-skills/16-01-SUMMARY.md`
</output>

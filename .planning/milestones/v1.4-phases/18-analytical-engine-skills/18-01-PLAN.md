---
phase: 18-analytical-engine-skills
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - finance-mcp-plugin/skills/private-equity/prospect-scoring/SKILL.md
  - finance-mcp-plugin/skills/private-equity/liquidity-risk/SKILL.md
  - finance-mcp-plugin/skills/private-equity/pipeline-profiling/SKILL.md
  - finance-mcp-plugin/skills/private-equity/public-comp-analysis/SKILL.md
  - finance-mcp-plugin/skills/private-equity/market-risk-scan/SKILL.md
autonomous: true
requirements: [SKILL-11, SKILL-12, SKILL-13, SKILL-14, SKILL-15]

must_haves:
  truths:
    - "PE professional can invoke /project:score-prospect and the skill trains an ML classifier on CRM CSV via investor_classifier, then scores prospects with confidence via classify_investor"
    - "PE professional can invoke /project:liquidity-risk and the skill trains a regression model on portfolio data via liquidity_predictor and returns predicted liquidity risk via predict_liquidity"
    - "PE professional can invoke /project:profile-pipeline and receive a full EDA report (completeness, distributions, outliers, data quality) on CRM CSV via ingest_csv"
    - "PE professional can invoke /project:public-comps and receive comparison chart and correlation heatmap for public market comps via compare_tickers and correlation_map"
    - "PE professional can invoke /project:market-risk and receive Sharpe ratio, drawdown, and beta for public benchmarks via get_volatility, get_risk_metrics, and analyze_stock"
    - "Each skill SKILL.md is 200-400 lines with MCP tool integration sections"
  artifacts:
    - path: "finance-mcp-plugin/skills/private-equity/prospect-scoring/SKILL.md"
      provides: "ML prospect scoring skill"
      contains: "investor_classifier"
      min_lines: 200
    - path: "finance-mcp-plugin/skills/private-equity/prospect-scoring/SKILL.md"
      provides: "Prospect scoring also uses classify_investor"
      contains: "classify_investor"
    - path: "finance-mcp-plugin/skills/private-equity/liquidity-risk/SKILL.md"
      provides: "Liquidity risk prediction skill"
      contains: "liquidity_predictor"
      min_lines: 200
    - path: "finance-mcp-plugin/skills/private-equity/liquidity-risk/SKILL.md"
      provides: "Liquidity risk also uses predict_liquidity"
      contains: "predict_liquidity"
    - path: "finance-mcp-plugin/skills/private-equity/pipeline-profiling/SKILL.md"
      provides: "Pipeline profiling EDA skill"
      contains: "ingest_csv"
      min_lines: 200
    - path: "finance-mcp-plugin/skills/private-equity/public-comp-analysis/SKILL.md"
      provides: "Public comp analysis skill"
      contains: "compare_tickers"
      min_lines: 200
    - path: "finance-mcp-plugin/skills/private-equity/public-comp-analysis/SKILL.md"
      provides: "Public comp analysis also uses correlation_map"
      contains: "correlation_map"
    - path: "finance-mcp-plugin/skills/private-equity/market-risk-scan/SKILL.md"
      provides: "Market risk scan skill"
      contains: "get_volatility"
      min_lines: 200
    - path: "finance-mcp-plugin/skills/private-equity/market-risk-scan/SKILL.md"
      provides: "Market risk scan also uses get_risk_metrics"
      contains: "get_risk_metrics"
    - path: "finance-mcp-plugin/skills/private-equity/market-risk-scan/SKILL.md"
      provides: "Market risk scan also uses analyze_stock"
      contains: "analyze_stock"
  key_links:
    - from: "finance-mcp-plugin/skills/private-equity/prospect-scoring/SKILL.md"
      to: "MCP investor_classifier + classify_investor tools"
      via: "skill trains classifier then scores prospects"
      pattern: "investor_classifier.*classify_investor"
    - from: "finance-mcp-plugin/skills/private-equity/liquidity-risk/SKILL.md"
      to: "MCP liquidity_predictor + predict_liquidity tools"
      via: "skill trains regression model then predicts risk"
      pattern: "liquidity_predictor.*predict_liquidity"
    - from: "finance-mcp-plugin/skills/private-equity/pipeline-profiling/SKILL.md"
      to: "MCP ingest_csv tool"
      via: "skill profiles CRM CSV exports"
      pattern: "ingest_csv"
    - from: "finance-mcp-plugin/skills/private-equity/public-comp-analysis/SKILL.md"
      to: "MCP compare_tickers + correlation_map tools"
      via: "skill generates comparison charts and heatmaps"
      pattern: "compare_tickers.*correlation_map"
    - from: "finance-mcp-plugin/skills/private-equity/market-risk-scan/SKILL.md"
      to: "MCP get_volatility + get_risk_metrics + analyze_stock tools"
      via: "skill retrieves volatility, risk metrics, and analysis"
      pattern: "get_volatility.*get_risk_metrics.*analyze_stock"
---

<objective>
Create 5 analytical engine PE skills (prospect-scoring, liquidity-risk, pipeline-profiling, public-comp-analysis, market-risk-scan) as SKILL.md files with heavy MCP tool integration. These are the skills that differentiate our plugin from Anthropic's vanilla plugin pattern -- they invoke live ML models and market data tools.

Purpose: PE professionals need analytical capabilities that go beyond static frameworks. These skills train ML models on real data, score prospects with confidence levels, predict liquidity risk, profile deal pipelines, compare public comps, and scan market risk -- all via MCP tool calls.
Output: 5 skill SKILL.md files (200-400 lines each) under finance-mcp-plugin/skills/private-equity/.
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/17-portfolio-and-value-creation-skills/17-02-SUMMARY.md
</context>

<interfaces>
<!-- Existing SKILL.md pattern (established in Phase 16, continued in Phase 17): -->
```yaml
---
name: [skill-name]
description: Use when a PE professional needs to [trigger phrases]...
version: 1.0.0
---
```

<!-- Each skill has: -->
<!-- 1. Role intro paragraph (2-3 sentences) -->
<!-- 2. Intent classification table (Intent | Trigger Phrases | Action) -->
<!-- 3. Workflow phases with numbered steps -->
<!-- 4. MCP tool integration sections (exact tool name + usage instructions + parameters) -->
<!-- 5. Output format specifications (tables, templates) -->
<!-- 6. Error handling / edge cases -->

<!-- KEY DIFFERENCE for Phase 18: These skills are MCP-HEAVY -->
<!-- Every skill uses at least one MCP tool. Two skills chain multiple tools (train -> predict). -->

<!-- Available MCP tools for analytical engine skills: -->
- `investor_classifier` -- Train ML classifier on CRM CSV (params: csv_path, target_column, features)
- `classify_investor` -- Score individual prospects using trained classifier (params: investor_data)
- `liquidity_predictor` -- Train regression model on portfolio data (params: csv_path, target_column, features)
- `predict_liquidity` -- Predict liquidity risk using trained model (params: portfolio_data)
- `ingest_csv` -- Profile CSV: completeness, distributions, outliers, data quality (params: csv_path)
- `compare_tickers` -- Compare stock prices for multiple tickers (params: tickers, start_date)
- `correlation_map` -- Generate correlation heatmap for tickers (params: tickers, start_date)
- `get_volatility` -- Historical volatility analysis (params: ticker, start_date)
- `get_risk_metrics` -- Sharpe ratio, max drawdown, beta (params: ticker, start_date, benchmark)
- `analyze_stock` -- Comprehensive stock analysis (params: ticker, start_date)

<!-- Reference: deal-sourcing/SKILL.md is 328 lines, ic-memo is 394 lines -- target similar density -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create prospect-scoring and liquidity-risk skills (ML-chain skills)</name>
  <files>
    finance-mcp-plugin/skills/private-equity/prospect-scoring/SKILL.md,
    finance-mcp-plugin/skills/private-equity/liquidity-risk/SKILL.md
  </files>
  <action>
Create two SKILL.md files (200-400 lines each) following the established pattern. These are the two ML-chain skills that train models then use them for prediction -- the most complex analytical engine skills.

**1. prospect-scoring/SKILL.md (SKILL-11)**
- Frontmatter: name `prospect-scoring`, description mentioning training ML classifier on CRM exports, scoring individual prospects with confidence, investor classification, deal pipeline scoring
- Role intro: You are a PE prospect scoring specialist who uses machine learning to classify and score potential investment targets and LP prospects. You train classifiers on historical CRM data and apply them to score new prospects with confidence levels.
- Intent classification table:
  - `train-classifier` -- "train on CRM data", "build a model", "train classifier", "learn from our data", "fit the model" -- Call MCP `investor_classifier` to train ML classifier on CRM CSV export
  - `score-prospect` -- "score this prospect", "classify this investor", "how does this one look", "score the lead", "rate this LP" -- Call MCP `classify_investor` to score an individual prospect using the trained model
  - `batch-score` -- "score all prospects", "rank the pipeline", "batch classify", "score the list" -- Loop `classify_investor` across a list of prospects and rank by confidence
  - `model-diagnostics` -- "how good is the model", "accuracy", "feature importance", "model performance" -- Analyze classifier metrics (accuracy, precision, recall, feature weights)
  - `retrain` -- "retrain", "update the model", "new data available", "refresh scoring" -- Re-train classifier with updated CRM data and compare to previous model
- MCP integration (CRITICAL -- this is the analytical engine):
  - Step 1: `investor_classifier` -- Train the ML model. Parameters: csv_path (path to CRM CSV export), target_column (the label column, e.g., "converted", "invested", "tier"), features (list of predictor columns). Show exact MCP call syntax. Explain that this trains a model and stores it for subsequent classify_investor calls.
  - Step 2: `classify_investor` -- Score individual prospects. Parameters: investor_data (dict of feature values for one prospect). Returns: classification label, confidence score (0-1), feature contributions. Show exact MCP call syntax with example input/output.
- Training data requirements: CRM CSV must have a target column (binary or categorical) and feature columns (firm_size, sector, check_size, geography, fund_stage, prior_investments, etc.). Minimum rows guidance (50+ for basic, 200+ for reliable).
- Scoring output format: Table with prospect name, predicted class, confidence %, top contributing features, recommended action (pursue/nurture/pass based on confidence thresholds)
- Confidence thresholds: High (>80%) = pursue immediately, Medium (50-80%) = nurture, Low (<50%) = pass or revisit
- Error handling: CSV missing target column, too few rows, imbalanced classes, missing feature values
- Output formats: Training summary (accuracy, features used, sample size), individual score card, batch ranking table, model diagnostics report

**2. liquidity-risk/SKILL.md (SKILL-12)**
- Frontmatter: name `liquidity-risk`, description mentioning training regression model on portfolio data, predicting liquidity risk per target, portfolio liquidity scoring
- Role intro: You are a PE liquidity risk specialist who uses machine learning regression to predict liquidity risk scores for portfolio companies. You train models on historical portfolio performance data and predict risk for current or prospective investments.
- Intent classification table:
  - `train-model` -- "train liquidity model", "build risk model", "fit on portfolio data", "train predictor" -- Call MCP `liquidity_predictor` to train regression model on portfolio CSV
  - `predict-risk` -- "predict liquidity risk", "what's the risk score", "how liquid is this", "risk assessment" -- Call MCP `predict_liquidity` to predict risk for a specific company
  - `portfolio-scan` -- "scan the portfolio", "rank by risk", "which companies are at risk", "risk heatmap" -- Loop `predict_liquidity` across portfolio and rank by predicted risk score
  - `risk-factors` -- "what drives risk", "risk factors", "feature importance", "why is it risky" -- Analyze regression coefficients and feature importance for risk drivers
  - `stress-test` -- "stress test", "worst case", "scenario analysis", "what if revenue drops" -- Modify input features and re-predict to simulate stress scenarios
- MCP integration (CRITICAL):
  - Step 1: `liquidity_predictor` -- Train the regression model. Parameters: csv_path (path to portfolio CSV), target_column (liquidity metric, e.g., "days_to_liquidity", "liquidity_score", "cash_runway_months"), features (predictor columns). Explain that this fits a regression model for continuous risk prediction.
  - Step 2: `predict_liquidity` -- Predict risk for a company. Parameters: portfolio_data (dict of feature values). Returns: predicted liquidity score, confidence interval, risk factors. Show exact MCP call syntax with example.
- Training data requirements: Portfolio CSV needs a continuous target column (liquidity metric) and features (revenue_growth, burn_rate, cash_balance, debt_ratio, customer_concentration, sector, etc.). Minimum rows guidance.
- Risk scoring framework: Map predicted score to risk tiers -- Low Risk (score > 0.7), Moderate Risk (0.4-0.7), High Risk (< 0.4). Each tier has recommended actions.
- Stress testing: Show how to modify input features (e.g., revenue -20%, burn rate +30%) and re-predict to model downside scenarios
- Error handling: Missing features, target column not continuous, insufficient training data, extreme predictions
- Output formats: Training summary (R-squared, RMSE, features), individual risk scorecard, portfolio risk heatmap, stress test comparison table
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
skills_dir = Path('finance-mcp-plugin/skills/private-equity')
checks = {
    'prospect-scoring': ['investor_classifier', 'classify_investor'],
    'liquidity-risk': ['liquidity_predictor', 'predict_liquidity'],
}
for skill, refs in checks.items():
    f = skills_dir / skill / 'SKILL.md'
    assert f.exists(), f'Missing: {f}'
    text = f.read_text()
    lines = text.strip().split('\n')
    assert len(lines) >= 200, f'{skill} too short: {len(lines)} lines'
    assert len(lines) <= 400, f'{skill} too long: {len(lines)} lines'
    assert text.startswith('---'), f'{skill} missing YAML frontmatter'
    for ref in refs:
        assert ref in text, f'{skill} missing MCP reference: {ref}'
print('prospect-scoring and liquidity-risk validated')
"
    </automated>
  </verify>
  <done>prospect-scoring/SKILL.md (200-400 lines) references both investor_classifier and classify_investor with train-then-score workflow. liquidity-risk/SKILL.md (200-400 lines) references both liquidity_predictor and predict_liquidity with train-then-predict workflow. Both have YAML frontmatter and intent classification tables.</done>
</task>

<task type="auto">
  <name>Task 2: Create pipeline-profiling, public-comp-analysis, and market-risk-scan skills</name>
  <files>
    finance-mcp-plugin/skills/private-equity/pipeline-profiling/SKILL.md,
    finance-mcp-plugin/skills/private-equity/public-comp-analysis/SKILL.md,
    finance-mcp-plugin/skills/private-equity/market-risk-scan/SKILL.md
  </files>
  <action>
Create three SKILL.md files (200-400 lines each) following the established pattern.

**3. pipeline-profiling/SKILL.md (SKILL-13)**
- Frontmatter: name `pipeline-profiling`, description mentioning full EDA on CRM CSV exports, completeness analysis, distributions, outliers, data quality flags, pipeline health assessment
- Role intro: You are a PE pipeline profiling specialist who uses data profiling to assess deal pipeline quality. You run exploratory data analysis on CRM exports to surface completeness gaps, distribution anomalies, outliers, and data quality issues that affect deal flow decisions.
- Intent classification table:
  - `profile-pipeline` -- "profile this pipeline", "EDA on our CRM", "data quality check", "what's in this CSV", "explore the pipeline data" -- Call MCP `ingest_csv` to run full EDA on CRM CSV export
  - `completeness-audit` -- "what's missing", "completeness", "data gaps", "which fields are empty", "fill rate" -- Focus on field completeness analysis from ingest_csv results
  - `distribution-analysis` -- "distribution", "how is the pipeline distributed", "sector breakdown", "size distribution", "stage distribution" -- Analyze distributions of key pipeline fields (sector, stage, deal size, geography)
  - `outlier-detection` -- "outliers", "unusual deals", "anomalies", "suspicious entries", "weird data" -- Surface outlier deals from ingest_csv profiling results
  - `pipeline-health` -- "pipeline health", "is our pipeline good", "pipeline score", "deal flow quality" -- Synthesize all EDA findings into a pipeline health scorecard
- MCP integration:
  - `ingest_csv` -- Profile the CRM CSV. Parameters: csv_path (path to CRM export). Returns: column summary (types, null counts, unique values), statistical summary (mean, median, std for numerics), distribution info, potential data quality issues. Show exact MCP call syntax.
  - Interpret results: Map ingest_csv output to PE-relevant insights (e.g., high null rate in "revenue" column = deals missing financials, skewed deal size distribution = pipeline concentration risk)
- Pipeline health scorecard: Completeness score (% of key fields populated across deals), Distribution score (concentration risk -- single sector/geography >50% of pipeline), Data quality score (outlier %, type mismatches, duplicate detection), Volume score (total deals vs target)
- Key fields for PE pipeline: Company name, sector, geography, deal size, stage (sourced/screened/LOI/signed), revenue, EBITDA, source channel, date added, last activity date
- Data quality flags: Missing revenue/EBITDA on >20% of deals, stale deals (no activity >90 days), duplicate company names, inconsistent sector labels
- Error handling: CSV read failure, no rows, all columns empty, non-CSV file
- Output formats: EDA summary report, completeness table, distribution charts spec, outlier list, pipeline health scorecard

**4. public-comp-analysis/SKILL.md (SKILL-14)**
- Frontmatter: name `public-comp-analysis`, description mentioning public market comparables, comparison charts, correlation heatmaps, valuation context, peer analysis
- Role intro: You are a PE public comps specialist who uses market data tools to build comparable company analyses for valuation context. You generate price comparison charts and correlation heatmaps to identify how public peers trade relative to each other and to potential PE targets.
- Intent classification table:
  - `compare-comps` -- "compare these comps", "peer comparison", "how do these companies trade", "price comparison", "comp chart" -- Call MCP `compare_tickers` to generate comparison chart
  - `correlation-analysis` -- "correlation", "how correlated are they", "correlation heatmap", "diversification", "co-movement" -- Call MCP `correlation_map` to generate correlation heatmap
  - `valuation-context` -- "valuation context", "what's the market paying", "trading multiples", "market valuation", "public peers" -- Combine compare_tickers and correlation_map to frame valuation context for a PE deal
  - `sector-comps` -- "sector comps", "industry peers", "who are the public peers", "comparable companies" -- Help identify appropriate public comp tickers for a given PE target's sector
  - `relative-performance` -- "relative performance", "outperformer", "underperformer", "which comp is best", "performance ranking" -- Rank comps by relative price performance from compare_tickers data
- MCP integration:
  - `compare_tickers` -- Generate price comparison chart. Parameters: tickers (list of 2-8 ticker symbols, e.g., ["AAPL", "MSFT", "GOOGL"]), start_date (comparison period start). Returns: normalized price chart showing relative performance. Show exact MCP call syntax with example.
  - `correlation_map` -- Generate correlation heatmap. Parameters: tickers (same list), start_date (same period). Returns: pairwise correlation matrix heatmap. Show exact MCP call syntax.
  - Workflow: First call compare_tickers for visual price comparison, then call correlation_map for statistical co-movement analysis. Frame results in PE context (diversification benefits, sector beta, relative valuation).
- PE context framing: How to use public comp data for PE valuation -- EV/Revenue and EV/EBITDA multiple ranges from public peers, applying illiquidity discount, size premium/discount, growth rate differential adjustments
- Ticker selection guidance: Map PE target sectors to relevant public comp tickers (e.g., B2B SaaS target -> CRM, NOW, HUBS, DDOG, ZS)
- Output formats: Comp comparison chart (from compare_tickers), correlation heatmap (from correlation_map), valuation context summary table, sector comp recommendations

**5. market-risk-scan/SKILL.md (SKILL-15)**
- Frontmatter: name `market-risk-scan`, description mentioning Sharpe ratio, drawdown, beta for public benchmarks, portfolio company market risk, volatility analysis
- Role intro: You are a PE market risk specialist who uses market data tools to assess public market risk for portfolio companies and their comparable benchmarks. You pull volatility, risk metrics, and comprehensive stock analysis to contextualize portfolio risk in public market terms.
- Intent classification table:
  - `risk-scan` -- "market risk scan", "risk metrics", "Sharpe ratio", "how risky is the market", "benchmark risk" -- Call MCP `get_risk_metrics` for Sharpe, drawdown, beta on specified tickers
  - `volatility-check` -- "volatility", "how volatile", "historical vol", "vol analysis" -- Call MCP `get_volatility` for historical volatility analysis
  - `full-analysis` -- "full analysis", "deep dive", "comprehensive analysis", "analyze this stock" -- Call MCP `analyze_stock` for comprehensive stock analysis
  - `benchmark-risk` -- "benchmark comparison", "vs S&P", "vs index", "relative risk", "beta to market" -- Compare portfolio company public peers against market benchmarks (SPY, QQQ)
  - `risk-report` -- "risk report", "risk summary", "quarterly risk update", "risk dashboard" -- Generate comprehensive market risk report combining all three tools
- MCP integration (three tools in sequence):
  - `get_volatility` -- Historical volatility. Parameters: ticker (e.g., "AAPL"), start_date. Returns: historical volatility metrics, rolling volatility. Show exact MCP call syntax.
  - `get_risk_metrics` -- Risk-adjusted metrics. Parameters: ticker, start_date, benchmark (e.g., "SPY"). Returns: Sharpe ratio, max drawdown, beta, alpha. Show exact MCP call syntax.
  - `analyze_stock` -- Comprehensive analysis. Parameters: ticker, start_date. Returns: price analysis, returns analysis, risk profile. Show exact MCP call syntax.
  - Workflow: For a full risk scan, call get_volatility first (quick vol check), then get_risk_metrics (risk-adjusted view), then analyze_stock (comprehensive). For quick scans, just get_risk_metrics.
- Risk interpretation for PE: How to translate public market risk metrics to PE context -- high beta means cyclical exposure, high drawdown means tail risk, low Sharpe means poor risk-adjusted returns relative to alternatives, volatility trends signal sector stress
- Benchmark selection: SPY (broad market), QQQ (tech-heavy), XLF (financials), XLV (healthcare), XLI (industrials) -- map portfolio company sectors to appropriate benchmarks
- Risk thresholds for PE context: Sharpe > 1.0 (good), 0.5-1.0 (acceptable), < 0.5 (poor risk-adjusted return). Max drawdown > 30% (high tail risk). Beta > 1.3 (highly cyclical).
- Error handling: Invalid ticker, no data for date range, benchmark not found
- Output formats: Risk metrics summary table, volatility trend, comprehensive analysis report, multi-ticker risk comparison table
  </action>
  <verify>
    <automated>python3 -c "
from pathlib import Path
skills_dir = Path('finance-mcp-plugin/skills/private-equity')
checks = {
    'pipeline-profiling': ['ingest_csv'],
    'public-comp-analysis': ['compare_tickers', 'correlation_map'],
    'market-risk-scan': ['get_volatility', 'get_risk_metrics', 'analyze_stock'],
}
for skill, refs in checks.items():
    f = skills_dir / skill / 'SKILL.md'
    assert f.exists(), f'Missing: {f}'
    text = f.read_text()
    lines = text.strip().split('\n')
    assert len(lines) >= 200, f'{skill} too short: {len(lines)} lines'
    assert len(lines) <= 400, f'{skill} too long: {len(lines)} lines'
    assert text.startswith('---'), f'{skill} missing YAML frontmatter'
    for ref in refs:
        assert ref in text, f'{skill} missing MCP reference: {ref}'
print('pipeline-profiling, public-comp-analysis, and market-risk-scan validated')
"
    </automated>
  </verify>
  <done>pipeline-profiling/SKILL.md references ingest_csv. public-comp-analysis/SKILL.md references both compare_tickers and correlation_map. market-risk-scan/SKILL.md references get_volatility, get_risk_metrics, and analyze_stock. All 200-400 lines with YAML frontmatter and intent classification tables.</done>
</task>

</tasks>

<verification>
All analytical engine skill requirements verified:
- SKILL-11: prospect-scoring skill with investor_classifier + classify_investor (ML train-then-score chain)
- SKILL-12: liquidity-risk skill with liquidity_predictor + predict_liquidity (ML train-then-predict chain)
- SKILL-13: pipeline-profiling skill with ingest_csv (full EDA)
- SKILL-14: public-comp-analysis skill with compare_tickers + correlation_map
- SKILL-15: market-risk-scan skill with get_volatility + get_risk_metrics + analyze_stock
</verification>

<success_criteria>
1. All 5 SKILL.md files exist at finance-mcp-plugin/skills/private-equity/{name}/SKILL.md
2. Each skill is 200-400 lines with proper YAML frontmatter (name, description, version)
3. prospect-scoring references both investor_classifier and classify_investor
4. liquidity-risk references both liquidity_predictor and predict_liquidity
5. pipeline-profiling references ingest_csv
6. public-comp-analysis references both compare_tickers and correlation_map
7. market-risk-scan references get_volatility, get_risk_metrics, and analyze_stock
</success_criteria>

<output>
After completion, create `.planning/phases/18-analytical-engine-skills/18-01-SUMMARY.md`
</output>

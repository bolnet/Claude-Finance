---
phase: 14-content-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.html
  - docs/assets/css/style.css
autonomous: true
requirements: [LAND-01, LAND-02, LAND-03, LAND-04]

must_haves:
  truths:
    - "Landing page hero states the value proposition in outcome-led language above the fold"
    - "Landing page shows at least one real chart output image as visual proof"
    - "Landing page includes 6 role-based entry points linking to specific walkthrough card anchors"
    - "Landing page displays a stats bar with accurate credibility numbers (11 tools, 6 walkthroughs)"
  artifacts:
    - path: "docs/index.html"
      provides: "Full landing page content replacing stub"
      contains: "stats-bar"
    - path: "docs/assets/css/style.css"
      provides: "New CSS classes for stats-bar, role-entry-points, role-grid, role-card, feature-category, feature-list"
      contains: "stats-bar"
  key_links:
    - from: "docs/index.html"
      to: "docs/walkthroughs.html"
      via: "role entry point anchor links"
      pattern: "walkthroughs\\.html#"
---

<objective>
Replace the stub landing page with full content: outcome-led hero, stats bar, "what it does" columns, role entry points, and proof chart images. Add all new CSS component classes to the shared stylesheet so Plans 02 and 03 can use them without modifying style.css.

Purpose: The landing page is the first impression for finance professionals. It must communicate value in 10 seconds: what they GET (not what the product IS), visual proof via real charts, and clear role-based navigation.

Output: Complete index.html with 6 content sections + style.css extended with all Phase 14 CSS classes.
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-content-pages/14-RESEARCH.md
@docs/index.html
@docs/assets/css/style.css
</context>

<interfaces>
<!-- CSS classes from Phase 13 already in style.css (executor should NOT recreate): -->
From docs/assets/css/style.css:
- `.hero`, `.hero h1`, `.hero p` — hero section layout and typography
- `.cta-primary` — red (#e94560) button
- `.hero-image` — max 800px, box-shadow, centered
- `.page-stub` — placeholder class (to be REMOVED from index.html)
- `nav`, `.nav-brand`, `.nav-toggle`, `.nav-links` — sticky nav bar
- `footer` — border-top disclaimer

Brand colors: #0f3460 (dark blue), #e94560 (red), #1a1a2e (navy text), #ffffff (white)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend style.css with all Phase 14 component classes</name>
  <files>docs/assets/css/style.css</files>
  <action>
Append the following CSS component classes to the END of style.css (after the footer section). Do NOT modify any existing CSS rules. Add these new sections in order:

1. **Stats bar** (`.stats-bar`, `.stat strong`, `.stat span`) — flexbox row, centered, grey background (#f8f9fa), wrap on mobile. Use the exact CSS from the research Code Examples section.

2. **Feature columns** (`.feature-columns`) — 3-column grid on desktop (`repeat(auto-fit, minmax(280px, 1fr))`), single column on mobile. Each column has a heading and paragraph.

3. **Role entry points** (`.role-entry-points`, `.role-entry`) — grid of 6 clickable link blocks. Each is a bordered card with strong title + span subtitle. Hover: border color changes to #e94560.

4. **Feature category** (`.feature-category`, `.feature-category h2`, `.category-intro`, `.feature-list`, `.feature-item`) — category heading with red bottom border, grid of tool cards. Use exact CSS from research Code Examples.

5. **Role card grid** (`.role-grid`, `.role-card`, `.role-card h3`, `.role-card .scenario`, `.role-card .example-prompt`, `.role-card img`, `.walkthrough-cta`, `.walkthrough-cta code`) — bordered cards with scenario text, red-left-bordered blockquote, lazy images. Use exact CSS from research Code Examples.

6. **Content section** (`.content-section`) — generic section with 3rem bottom margin and max-width 1100px centering for sections used outside `<main>` (stats-bar sits outside main for full-width background).

7. **Proof section** (`.proof-section`) — centered text, image with caption below.

8. **Attribution callout** (`.attribution`) — centered text block, slightly smaller font, grey color.

Add a responsive media query at 640px for `.stats-bar` to change gap from 3rem to 1.5rem and flex-direction to column on very narrow screens.

WHY all CSS goes in this task: Plans 02 and 03 (features.html, walkthroughs.html) depend on these classes. Adding them here means those plans do zero CSS work.
  </action>
  <verify>
    <automated>grep -c "stats-bar\|role-grid\|role-card\|feature-category\|feature-list\|role-entry" docs/assets/css/style.css</automated>
  </verify>
  <done>style.css contains all 7 new component class families. No existing CSS rules were modified.</done>
</task>

<task type="auto">
  <name>Task 2: Replace index.html stub with full landing page content</name>
  <files>docs/index.html</files>
  <action>
Replace the entire `<main>` section of index.html with 6 content sections. Keep the existing `<head>`, `<header>`, and `<footer>` unchanged (except: remove `loading="lazy"` from the hero image per research Pitfall 3).

Section 1 — **Hero** (inside `<main>`):
- h1: "Finance analysis in plain English — no Python required" (use `<br>` between the two halves for line break on desktop)
- p: "Describe what you need. The Finance AI Skill writes the code, runs the analysis, and explains the results — so you focus on the decision, not the data."
- CTA button: "Get Started Free" linking to getting-started.html
- Hero image: `assets/images/compare-tech-stocks.png` with descriptive alt text "AAPL, GOOGL, MSFT, and NVDA normalized price comparison over 6 months". NO `loading="lazy"` — this is above the fold.

Section 2 — **Stats bar** (OUTSIDE `<main>`, between main's hero and the next section, OR as a full-width section — use a `<section class="stats-bar">` directly after the hero section inside main):
- 4 stats: "11" + "built-in analyses" | "6" + "role walkthroughs" | "0" + "Python required" | "Free" + "open source"

Section 3 — **What it does** (`.feature-columns` grid):
- h2: "What it does"
- 3 columns:
  - "Market Analysis" — "Price charts, returns, volatility, risk metrics, peer comparisons, and correlation heatmaps — for any publicly traded stock."
  - "ML Workflows" — "Upload your data. Train regression and classification models. Score new observations. No Python setup, no sklearn boilerplate."
  - "Role Walkthroughs" — "Step-by-step scenarios for equity research, hedge funds, investment banking, FP&A, private equity, and accounting."

Section 4 — **Role entry points** (LAND-03):
- h2: "Built for how you work"
- 6 entry point links using `.role-grid` and `.role-entry` classes. Each links to `walkthroughs.html#[role-id]`:
  - Equity Research → `walkthroughs.html#equity-research` — "Coverage initiation, peer comps, risk profiling"
  - Hedge Fund → `walkthroughs.html#hedge-fund` — "Volatility regimes, pair trading, cross-sector correlation"
  - Investment Banking → `walkthroughs.html#investment-banking` — "Comparable company analysis, deal pitch exhibits"
  - FP&A → `walkthroughs.html#fpa` — "ERP data profiling, liquidity forecasting, budget variance"
  - Private Equity → `walkthroughs.html#private-equity` — "Deal scoring, IC memo prep, portfolio monitoring"
  - Accounting & Control → `walkthroughs.html#accounting` — "Transaction profiling, anomaly detection, ERP review"

Section 5 — **Proof section**:
- h2: "Real output from real data"
- Two images with captions:
  - `assets/images/volatility-analysis.png` alt="Stock volatility analysis with regime detection" + caption: "Volatility regime analysis — generated from a single natural-language prompt"
  - `assets/images/correlation-heatmap.png` alt="Cross-sector correlation heatmap for portfolio construction" + caption: "Correlation heatmap — critical for portfolio diversification decisions"
- Both images: `loading="lazy"` (below fold)

Section 6 — **Attribution callout**:
- p: "Built on the Python & Machine Learning for Finance curriculum. Free and open source."
- No link needed — just a text callout.

IMPORTANT ANTI-PATTERNS TO AVOID:
- Do NOT use root-absolute paths (no leading `/`). All paths must be relative: `assets/...`
- Do NOT put `loading="lazy"` on the hero image
- Do NOT use "Finance AI Skill" as the hero h1 — lead with the outcome
- Ensure all `&` characters in HTML text are encoded as `&amp;` (e.g., "FP&amp;A", "Accounting &amp; Control")
  </action>
  <verify>
    <automated>grep -c 'walkthroughs.html#' docs/index.html | xargs -I{} test {} -ge 6 && echo "PASS: 6+ role links" || echo "FAIL: fewer than 6 role links"; grep -c 'stats-bar' docs/index.html; grep -c 'loading="lazy"' docs/index.html | head -1; grep 'class="hero-image"' docs/index.html | grep -v 'loading="lazy"' && echo "PASS: hero image not lazy" || echo "FAIL: hero image is lazy"</automated>
  </verify>
  <done>index.html has: (1) outcome-led hero headline, (2) stats bar with 4 credibility numbers, (3) 3-column "what it does" section, (4) 6 role entry points each linking to walkthroughs.html#[role-id], (5) 2 proof chart images with captions, (6) attribution callout. Hero image does NOT have loading="lazy".</done>
</task>

</tasks>

<verification>
- `grep -c 'walkthroughs.html#' docs/index.html` returns 6 (one per role)
- `grep -c 'stats-bar' docs/index.html` returns at least 1
- `grep -c 'assets/images/' docs/index.html` returns at least 3 (hero + 2 proof images)
- `grep 'loading="lazy"' docs/index.html` does NOT match the hero image line
- `grep -c 'role-grid\|role-card\|feature-category\|stats-bar' docs/assets/css/style.css` returns 4+
- No root-absolute paths: `grep -c 'href="/' docs/index.html` returns 0
- No root-absolute paths: `grep -c 'src="/' docs/index.html` returns 0
</verification>

<success_criteria>
- Landing page hero communicates value in outcome-led language (no product-name-first headline)
- At least 3 real chart images embedded in index.html
- 6 role entry points link to walkthroughs.html with fragment anchors
- Stats bar displays 4 accurate credibility numbers
- All new CSS classes available for Plans 02 and 03
- Zero root-absolute paths in index.html
</success_criteria>

<output>
After completion, create `.planning/phases/14-content-pages/14-01-SUMMARY.md`
</output>

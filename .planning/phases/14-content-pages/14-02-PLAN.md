---
phase: 14-content-pages
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - docs/features.html
  - docs/walkthroughs.html
autonomous: true
requirements: [FEAT-01, FEAT-02, WALK-01, WALK-02]

must_haves:
  truths:
    - "Features page groups all 11 MCP tools by category (market analysis vs ML workflows) with plain-language outcome descriptions"
    - "Features page shows at least one chart image per tool category as visual proof"
    - "Walkthroughs page presents 6 role cards each with situation sentence, example prompt, and chart image"
    - "Each walkthrough card has an id attribute matching the anchor links from index.html"
  artifacts:
    - path: "docs/features.html"
      provides: "Full features page with 11 tools in 2 categories"
      contains: "feature-category"
    - path: "docs/walkthroughs.html"
      provides: "Full walkthroughs page with 6 role cards"
      contains: "role-card"
  key_links:
    - from: "docs/index.html"
      to: "docs/walkthroughs.html"
      via: "anchor links matching role card ids"
      pattern: "id=\"equity-research\"|id=\"hedge-fund\"|id=\"investment-banking\"|id=\"fpa\"|id=\"private-equity\"|id=\"accounting\""
    - from: "docs/features.html"
      to: "docs/assets/images/"
      via: "chart images per category"
      pattern: "assets/images/.*\\.png"
---

<objective>
Replace the stub content in features.html and walkthroughs.html with full page content. Features page organizes all 11 tools by category with plain-language descriptions and chart images. Walkthroughs page presents 6 role cards with scenario context sourced from the walkthrough command files.

Purpose: Finance professionals visiting Features see what the skill does in their language. Professionals visiting Walkthroughs see themselves in the scenario descriptions and understand what they would get.

Output: Complete features.html and walkthroughs.html replacing page-stub sections.
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-content-pages/14-RESEARCH.md
@.planning/phases/14-content-pages/14-01-SUMMARY.md
@docs/features.html
@docs/walkthroughs.html
@.claude/commands/walkthrough-equity-research.md
@.claude/commands/walkthrough-hedge-fund.md
@.claude/commands/walkthrough-investment-banking.md
@.claude/commands/walkthrough-accounting.md
@.claude/commands/walkthrough-fpa.md
@.claude/commands/walkthrough-private-equity.md
</context>

<interfaces>
<!-- CSS classes available from Plan 01 (in docs/assets/css/style.css) -->
Feature page classes:
- `.feature-category` — section wrapper with bottom margin
- `.feature-category h2` — dark blue heading with red bottom border
- `.category-intro` — grey intro paragraph
- `.feature-list` — CSS grid, auto-fit columns minmax(260px, 1fr)
- `.feature-item` — light grey card with strong name + p description

Walkthrough page classes:
- `.role-grid` — CSS grid, auto-fit columns minmax(300px, 1fr)
- `.role-card` — bordered card with padding, white background
- `.role-card h3` — dark blue role title
- `.role-card .scenario` — grey scenario paragraph
- `.role-card .example-prompt` — blockquote with red left border, italic
- `.role-card img` — rounded image
- `.walkthrough-cta` — small grey text for command reference
- `.walkthrough-cta code` — inline code styling

Curated images available (docs/assets/images/):
- compare-tech-stocks.png — IB walkthrough card
- compare-semiconductor-stocks.png — Equity Research card + Market Analysis feature section
- correlation-heatmap.png — Hedge Fund card + Market Analysis feature section
- volatility-analysis.png — (used on landing page proof section)
- confusion-matrix.png — Accounting card + ML Workflow feature section
- feature-importance.png — Private Equity card + ML Workflow feature section
- eda-credit-risk.png — FP&A card
- residual-plot.png — (secondary, available if needed)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace features.html stub with full tool catalog</name>
  <files>docs/features.html</files>
  <action>
Replace the `<main>` section of features.html. Keep head/header/footer unchanged.

Page structure:

**Page intro:**
- h1: "Every analysis, one sentence away"
- p: "The Finance AI Skill includes 11 built-in analyses — from price charts to machine learning models. Describe what you need in plain English. The skill writes the code, runs it, and explains the results."

**Category 1: Market Analysis Tools** (`.feature-category`):
- h2: "Market Analysis"
- `.category-intro`: "Six tools for any publicly traded stock. Price charts, returns, volatility, risk metrics, peer comparisons, and correlation analysis — generated from a single natural-language prompt."
- Representative image: `assets/images/compare-semiconductor-stocks.png` alt="Semiconductor peer group price comparison — NVDA, AMD, INTC, AVGO, QCOM" with `loading="lazy"`
- `.feature-list` grid with 6 `.feature-item` entries:
  1. **Price Chart** — "See any stock's price history — 1 month to 5 years — as a publication-ready chart"
  2. **Returns Analysis** — "Calculate daily returns, cumulative performance, and compare to any benchmark"
  3. **Volatility Analysis** — "Measure price volatility and identify regime changes — key for position sizing and risk review"
  4. **Risk Metrics** — "Get Sharpe ratio, max drawdown, and beta in one command — no spreadsheet needed"
  5. **Multi-Ticker Comparison** — "Compare up to 5 stocks on a normalized basis — ideal for peer analysis and comps"
  6. **Correlation Heatmap** — "See how your holdings move together — critical for portfolio construction and diversification"

**Category 2: ML Workflow Tools** (`.feature-category`):
- h2: "ML Workflows"
- `.category-intro`: "Five tools for your own data. Upload a CSV from your ERP, CRM, or portfolio system. Train regression and classification models. Score new observations. No Python setup, no scikit-learn boilerplate."
- Representative image: `assets/images/confusion-matrix.png` alt="Investor classification confusion matrix — model accuracy visualization" with `loading="lazy"`
- `.feature-list` grid with 5 `.feature-item` entries:
  1. **Data Profiling** — "Upload your ERP or portfolio CSV — the skill profiles the data, flags anomalies, and identifies modeling targets"
  2. **Liquidity Risk Model** — "Train a regression model on your data to predict liquidity risk scores — no Python, no sklearn setup"
  3. **Liquidity Scoring** — "Score a new client or position against your trained model — single command, instant result"
  4. **Investor Segmentation** — "Train a classification model to segment investors by profile — outputs labeled segments and feature importance"
  5. **Investor Classification** — "Classify a new investor into a segment using your trained model"

**CTA section at bottom:**
- p: "Ready to try it?" + CTA link "Get Started Free" (`.cta-primary`) linking to `getting-started.html`

IMPORTANT:
- All descriptions are OUTCOME-LED (what the finance pro gets), not MECHANISM-LED (how it works internally)
- Do NOT mention MCP, FastMCP, stdio, transport, or scikit-learn in primary descriptions
- All image paths are relative (no leading `/`)
- All below-fold images have `loading="lazy"`
- Encode `&` as `&amp;` in HTML text
  </action>
  <verify>
    <automated>grep -c 'feature-item' docs/features.html | xargs -I{} test {} -ge 11 && echo "PASS: 11+ feature items" || echo "FAIL"; grep -c 'feature-category' docs/features.html | xargs -I{} test {} -ge 2 && echo "PASS: 2+ categories" || echo "FAIL"; grep -c 'assets/images/' docs/features.html | xargs -I{} test {} -ge 2 && echo "PASS: 2+ images" || echo "FAIL"</automated>
  </verify>
  <done>features.html contains 11 tool entries organized in 2 category sections, each category has a representative chart image, all descriptions use plain-language finance outcomes with no developer jargon.</done>
</task>

<task type="auto">
  <name>Task 2: Replace walkthroughs.html stub with 6 role cards</name>
  <files>docs/walkthroughs.html</files>
  <action>
Replace the `<main>` section of walkthroughs.html. Keep head/header/footer unchanged.

Page structure:

**Page intro:**
- h1: "See yourself in the scenario"
- p: "Each walkthrough drops you into a real finance situation — the kind you face every quarter. Pick your role, read the scenario, and see what the skill produces."

**Role card grid** (`.role-grid` inside a content section):

Create 6 `.role-card` divs. CRITICAL: Each card MUST have an `id` attribute matching the anchor links from index.html (LAND-03). The scenario text MUST be sourced from the walkthrough command files — do not paraphrase from memory.

Card 1 — **Equity Research Analyst** (`id="equity-research"`):
- h3: "Equity Research Analyst"
- `.scenario`: "Your head of research assigns you coverage of NVDA within the semiconductor sector. You need the quantitative backbone — price history, risk profile, and peer comps — before writing a single page of the initiation report."
- `.example-prompt` blockquote: "Compare NVDA against AMD, INTC, AVGO, and QCOM over the last year and show me correlation and risk metrics for each name."
- Image: `assets/images/compare-semiconductor-stocks.png` alt="NVDA, AMD, INTC, AVGO, and QCOM normalized price comparison — semiconductor peer group" `loading="lazy"`
- `.walkthrough-cta`: "Run `/walkthrough-equity-research` in Claude Code for the full scenario."

Card 2 — **Hedge Fund PM** (`id="hedge-fund"`):
- h3: "Hedge Fund Portfolio Manager"
- `.scenario`: "Your CIO wants a volatility regime assessment and pair trading opportunities across two sectors — semiconductors and mega-cap tech. You need to identify volatility dislocations, cross-sector diversification potential, and correlation-based pair candidates."
- `.example-prompt` blockquote: "Show me volatility regimes for NVDA, AMD, and AVGO over the last 6 months and compare cross-sector correlation against AAPL, MSFT, and GOOGL."
- Image: `assets/images/correlation-heatmap.png` alt="Cross-sector correlation heatmap — semiconductors vs mega-cap tech" `loading="lazy"`
- `.walkthrough-cta`: "Run `/walkthrough-hedge-fund` in Claude Code for the full scenario."

Card 3 — **Investment Banking Analyst** (`id="investment-banking"`):
- h3: "Investment Banking Analyst"
- `.scenario`: "Your Managing Director is pitching an acquisition advisory mandate for a mid-cap cloud infrastructure company. You need quantitative exhibits for the pitch book — performance comps, risk profiles, and correlation analysis for MSFT, GOOGL, AMZN, CRM, and ORCL."
- `.example-prompt` blockquote: "Compare MSFT, GOOGL, AMZN, CRM, and ORCL over the last 9 months with risk metrics and correlation analysis for the pitch deck."
- Image: `assets/images/compare-tech-stocks.png` alt="MSFT, GOOGL, AMZN, CRM, and ORCL price comparison — cloud/tech comps universe" `loading="lazy"`
- `.walkthrough-cta`: "Run `/walkthrough-investment-banking` in Claude Code for the full scenario."

Card 4 — **FP&amp;A Analyst** (`id="fpa"`):
- h3: "FP&amp;A Analyst"
- `.scenario`: "You are preparing quarterly budget variance analysis. Finance has exported a client portfolio dataset from the ERP system. Before building the forecast model, you need to profile the data quality, identify which columns drive liquidity risk, and run a regression model to generate budget projections."
- `.example-prompt` blockquote: "Profile this ERP export for data quality issues, then train a liquidity risk model and score our top 3 client segments."
- Image: `assets/images/eda-credit-risk.png` alt="ERP data profiling — credit risk distribution analysis" `loading="lazy"`
- `.walkthrough-cta`: "Run `/walkthrough-fpa` in Claude Code for the full scenario."

Card 5 — **Private Equity VP** (`id="private-equity"`):
- h3: "Private Equity VP"
- `.scenario`: "You are evaluating three acquisition prospects for Fund III with a $400M deployment target. The investment committee meets Friday. You need a data-driven ranking of three prospects — growth equity, leveraged buyout, and early-stage venture — before the IC deck is finalized."
- `.example-prompt` blockquote: "Train a scoring model on our deal database and classify these three prospects for the IC memo."
- Image: `assets/images/feature-importance.png` alt="Feature importance chart — investor classification model for deal scoring" `loading="lazy"`
- `.walkthrough-cta`: "Run `/walkthrough-private-equity` in Claude Code for the full scenario."

Card 6 — **Accounting Controller** (`id="accounting"`):
- h3: "Senior Controller"
- `.scenario`: "You are preparing for the quarterly close. The accounting team has exported a client portfolio dataset from the ERP system for consolidation review. Before signing off on the trial balance, you need to profile the transaction data, identify segment distributions, and run a classifier to validate client segments match expected patterns."
- `.example-prompt` blockquote: "Profile this ERP export for the quarterly close, flag any segment misclassifications, and validate the general ledger segments."
- Image: `assets/images/confusion-matrix.png` alt="Classification confusion matrix — segment validation for ERP consolidation" `loading="lazy"`
- `.walkthrough-cta`: "Run `/walkthrough-accounting` in Claude Code for the full scenario."

**CTA section at bottom:**
- p: "Ready to run a walkthrough?" + CTA link "Get Started Free" (`.cta-primary`) linking to `getting-started.html`

IMPORTANT:
- Each card MUST have an `id` attribute matching the anchor from index.html role entry points
- Scenario text is adapted from `.claude/commands/walkthrough-*.md` verbatim scenario blocks — do not invent new scenarios
- All image paths are relative (no leading `/`)
- All images have `loading="lazy"` (all below fold)
- Encode `&` as `&amp;` in HTML text (especially "FP&amp;A" and "Accounting &amp; Control")
  </action>
  <verify>
    <automated>grep -c 'role-card' docs/walkthroughs.html | xargs -I{} test {} -ge 6 && echo "PASS: 6+ role cards" || echo "FAIL"; grep -c 'id="equity-research"\|id="hedge-fund"\|id="investment-banking"\|id="fpa"\|id="private-equity"\|id="accounting"' docs/walkthroughs.html | xargs -I{} test {} -ge 6 && echo "PASS: 6 anchor ids" || echo "FAIL"; grep -c 'assets/images/' docs/walkthroughs.html | xargs -I{} test {} -ge 6 && echo "PASS: 6+ images" || echo "FAIL"; grep -c 'example-prompt' docs/walkthroughs.html | xargs -I{} test {} -ge 6 && echo "PASS: 6+ example prompts" || echo "FAIL"</automated>
  </verify>
  <done>walkthroughs.html contains 6 role cards, each with: (1) matching id anchor for deep-linking from index.html, (2) situation sentence sourced from walkthrough command files, (3) example prompt blockquote, (4) chart image with descriptive alt text. All images lazy-loaded.</done>
</task>

</tasks>

<verification>
- `grep -c 'feature-item' docs/features.html` returns 11+ (one per tool, accounting for class usage in CSS)
- `grep -c 'feature-category' docs/features.html` returns 2+ (market analysis + ML workflows)
- `grep -c 'role-card' docs/walkthroughs.html` returns 6+ (one per role)
- `grep -c 'id="equity-research"' docs/walkthroughs.html` returns 1
- `grep -c 'id="hedge-fund"' docs/walkthroughs.html` returns 1
- `grep -c 'id="investment-banking"' docs/walkthroughs.html` returns 1
- `grep -c 'id="fpa"' docs/walkthroughs.html` returns 1
- `grep -c 'id="private-equity"' docs/walkthroughs.html` returns 1
- `grep -c 'id="accounting"' docs/walkthroughs.html` returns 1
- No root-absolute paths: `grep -c 'href="/' docs/features.html docs/walkthroughs.html` returns 0
- No root-absolute paths: `grep -c 'src="/' docs/features.html docs/walkthroughs.html` returns 0
- No MCP jargon in primary descriptions: `grep -i 'mcp\|fastmcp\|stdio' docs/features.html` returns 0 hits in feature-item descriptions
</verification>

<success_criteria>
- Features page has 11 tools in 2 categories with plain-language outcome descriptions
- Each tool category has a representative chart image
- Walkthroughs page has 6 role cards with id anchors matching index.html links
- Each card has situation sentence, example prompt blockquote, and chart image
- Zero MCP/developer jargon in user-facing copy
- Zero root-absolute paths
</success_criteria>

<output>
After completion, create `.planning/phases/14-content-pages/14-02-SUMMARY.md`
</output>

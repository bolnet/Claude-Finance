---
phase: 07-ml-workflow-demos
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - tests/test_ml_demo_steps.py
autonomous: true
requirements:
  - MLWF-02
  - MLWF-03
  - MLWF-04

must_haves:
  truths:
    - "ingest_csv runs on demo/sample_portfolio.csv and returns cleaning/EDA output"
    - "liquidity_predictor trains on sample CSV and predict_liquidity scores a new client"
    - "investor_classifier trains on sample CSV and classify_investor classifies a new investor"
  artifacts:
    - path: "tests/test_ml_demo_steps.py"
      provides: "Integration tests proving all 4 ML tools work with sample CSV"
      min_lines: 40
  key_links:
    - from: "demo/sample_portfolio.csv"
      to: "src/finance_mcp/tools/csv_ingest.py"
      via: "ingest_csv('demo/sample_portfolio.csv')"
      pattern: "ingest_csv"
    - from: "demo/sample_portfolio.csv"
      to: "src/finance_mcp/tools/liquidity_model.py"
      via: "liquidity_predictor then predict_liquidity"
      pattern: "liquidity_predictor.*predict_liquidity"
    - from: "demo/sample_portfolio.csv"
      to: "src/finance_mcp/tools/investor_model.py"
      via: "investor_classifier then classify_investor"
      pattern: "investor_classifier.*classify_investor"
---

<objective>
Verify all 4 ML tools execute successfully on demo/sample_portfolio.csv, matching the exact invocations in demo.md Steps 9-11.

Purpose: Prove the sample CSV works end-to-end with all ML tools before the demo runs live.
Output: Integration tests that call each tool with the same arguments demo.md uses.
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ml-workflow-demos/07-01-SUMMARY.md

@.claude/commands/demo.md
@src/finance_mcp/tools/csv_ingest.py
@src/finance_mcp/tools/liquidity_model.py
@src/finance_mcp/tools/investor_model.py

<interfaces>
<!-- Exact tool signatures from source -->

From csv_ingest.py:
  def ingest_csv(csv_path: str, target_column: str = "") -> str

From liquidity_model.py:
  def liquidity_predictor(csv_path: str, target_column: str = "liquidity_risk") -> str
  def predict_liquidity(credit_score: float, debt_ratio: float, region: str, model_path: str = "") -> str

From investor_model.py:
  def investor_classifier(csv_path: str, target_column: str = "segment") -> str
  def classify_investor(age: float, income: float, risk_tolerance: float, product_preference: str, model_path: str = "") -> str

<!-- Demo.md Step 10 calls predict_liquidity with: credit_score=720, debt_ratio=0.35, region="Northeast" -->
<!-- Demo.md Step 11 calls classify_investor with: age=42, income=120000, risk_tolerance="moderate", product_preference="equities" -->
<!-- NOTE: demo.md passes risk_tolerance="moderate" (string) but function expects float. Test must verify this works or document needed fix. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration tests for ML demo steps 9-11</name>
  <files>tests/test_ml_demo_steps.py</files>
  <behavior>
    - Test 1 (Step 9): ingest_csv("demo/sample_portfolio.csv") returns a string containing "rows" and "columns" (EDA summary)
    - Test 2 (Step 10a): liquidity_predictor("demo/sample_portfolio.csv") returns a string containing "RMSE" and "R²" (or "R-squared" or "R2")
    - Test 3 (Step 10b): After training, predict_liquidity(credit_score=720, debt_ratio=0.35, region="Northeast") returns a string containing "liquidity risk" (case-insensitive)
    - Test 4 (Step 11a): investor_classifier("demo/sample_portfolio.csv") returns a string containing "accuracy" (case-insensitive)
    - Test 5 (Step 11b): After training, classify_investor(age=42, income=120000, risk_tolerance=0.5, product_preference="equities") returns a string containing a segment label
    - Test 6: Verify demo.md risk_tolerance parameter — demo.md passes "moderate" (string) but classify_investor expects float; document whether this needs a demo.md fix
  </behavior>
  <action>
Write tests/test_ml_demo_steps.py with all 6 tests. Tests call the Python functions directly (not via MCP).

IMPORTANT: demo.md Step 11 passes risk_tolerance="moderate" but classify_investor expects risk_tolerance: float. This is a schema mismatch. Test 6 should:
1. Call classify_investor(age=42, income=120000, risk_tolerance="moderate", product_preference="equities")
2. If it raises ValueError/TypeError, mark as xfail with reason="demo.md passes string but tool expects float — needs demo.md fix"
3. Document finding in test docstring

If the string-to-float conversion fails, do NOT fix demo.md in this plan (that file is not in files_modified). Instead, note it in the SUMMARY for the next plan to address.

Run all tests from the project root directory (cwd matters for relative CSV paths). Use: .venv/bin/python3 -m pytest tests/test_ml_demo_steps.py -v

Tests that train models may take 10-20 seconds. Use tmp_path for model artifacts to avoid polluting finance_output/.
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/test_ml_demo_steps.py -v</automated>
  </verify>
  <done>All 5 core tests pass (Tests 1-5). Test 6 either passes or is documented as xfail with clear explanation. All 4 ML tools proven to work with sample CSV using demo.md's exact parameters.</done>
</task>

</tasks>

<verification>
- ingest_csv produces EDA output from sample CSV
- liquidity_predictor trains and predict_liquidity scores a client
- investor_classifier trains and classify_investor classifies an investor
- Any demo.md parameter mismatches are documented
</verification>

<success_criteria>
- All 4 ML tool functions execute without error on demo/sample_portfolio.csv
- predict_liquidity and classify_investor produce meaningful predictions
- Parameter mismatches between demo.md and tool signatures are identified and documented
</success_criteria>

<output>
After completion, create `.planning/phases/07-ml-workflow-demos/07-02-SUMMARY.md`
</output>

---
phase: 07-ml-workflow-demos
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - .claude/commands/demo.md
autonomous: false
requirements:
  - MLWF-02
  - MLWF-03
  - MLWF-04

must_haves:
  truths:
    - "demo.md Steps 9-11 parameter schemas match tool function signatures exactly"
    - "Running /demo executes Steps 9-11 without skip messages"
    - "User sees plain-English explanations for CSV ingestion, regression, and classification"
  artifacts:
    - path: ".claude/commands/demo.md"
      provides: "Corrected ML demo steps with matching parameter schemas"
      contains: "sample_portfolio.csv"
  key_links:
    - from: ".claude/commands/demo.md"
      to: "demo/sample_portfolio.csv"
      via: "csv_path parameter in Steps 9-11"
      pattern: "demo/sample_portfolio.csv"
    - from: ".claude/commands/demo.md"
      to: "src/finance_mcp/tools/investor_model.py"
      via: "classify_investor call in Step 11"
      pattern: "classify_investor"
---

<objective>
Fix any parameter mismatches in demo.md Steps 9-11, then verify the full ML demo flow works end-to-end via human walkthrough.

Purpose: Ensure /demo Steps 9-11 actually execute without errors or skip messages now that the sample CSV exists.
Output: Corrected demo.md (if needed) and human-verified ML demo flow.
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ml-workflow-demos/07-01-SUMMARY.md
@.planning/phases/07-ml-workflow-demos/07-02-SUMMARY.md

@.claude/commands/demo.md
@src/finance_mcp/tools/investor_model.py

<interfaces>
<!-- classify_investor signature — risk_tolerance is float, not string -->
def classify_investor(age: float, income: float, risk_tolerance: float, product_preference: str, model_path: str = "") -> str
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix demo.md Step 11 parameter mismatch</name>
  <files>.claude/commands/demo.md</files>
  <action>
Read .claude/commands/demo.md. In Step 11, the classify_investor call passes risk_tolerance: "moderate" (a string), but the function signature expects risk_tolerance: float (0.0-1.0).

Fix Step 11 to pass risk_tolerance as a numeric value that represents "moderate" risk:
- Change: risk_tolerance: "moderate" --> risk_tolerance: 0.5

This aligns with the tool's actual API. The explanation text already says "moderate" — the parameter just needs to be numeric.

Also verify Steps 9 and 10 parameters match their tool signatures (from 07-02 SUMMARY findings). Fix any other mismatches found.

Do NOT change any other steps (1-8, 12+). Only touch Steps 9-11 parameter values.
  </action>
  <verify>
    <automated>grep -n "risk_tolerance" /Users/aarjay/Documents/machine_learning_skill/.claude/commands/demo.md</automated>
  </verify>
  <done>demo.md Step 11 passes risk_tolerance: 0.5 (float) instead of "moderate" (string). All Step 9-11 parameters match tool function signatures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of /demo ML steps</name>
  <files>.claude/commands/demo.md</files>
  <action>
Present the human verification checkpoint for Steps 9-11 of the /demo walkthrough.
  </action>
  <what-built>
Complete ML demo flow: sample CSV (07-01), integration tests (07-02), and demo.md parameter fixes (Task 1 above). Steps 9-11 of /demo should now execute without skip messages, producing:
- Step 9: CSV ingestion with column summary and EDA charts
- Step 10: Liquidity regression with RMSE/R-squared and a prediction
- Step 11: Investor classification with accuracy/CV scores and a segment label
  </what-built>
  <how-to-verify>
1. Open Claude Code in this project directory
2. Type `/demo` to start the walkthrough
3. Let it run through Steps 1-8 (market analysis — already verified in Phase 6)
4. Watch Steps 9-11 carefully:
   - Step 9: Should show "Loaded X rows and 9 columns from sample_portfolio.csv" with histogram charts
   - Step 10: Should show R-squared and RMSE values, then a liquidity risk prediction for credit_score=720
   - Step 11: Should show accuracy and cross-validation scores, then classify a 42-year-old investor into a segment
5. Verify all three steps produce plain-English explanations (not raw tracebacks)
6. Confirm the demo reaches the completion summary without errors
  </how-to-verify>
  <verify>User confirms Steps 9-11 execute with plain-English explanations</verify>
  <done>Human approves that all ML demo steps produce correct output with explanations</done>
  <resume-signal>Type "approved" if Steps 9-11 all execute with explanations, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- demo.md parameter schemas match tool signatures
- /demo Steps 9-11 execute without skip messages
- Plain-English explanations appear for all ML tools
</verification>

<success_criteria>
- demo.md risk_tolerance parameter fixed from string to float
- Human confirms /demo Steps 9-11 produce correct output with explanations
- All 4 ML workflow requirements (MLWF-01 through MLWF-04) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/07-ml-workflow-demos/07-03-SUMMARY.md`
</output>

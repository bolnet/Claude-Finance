---
phase: 04-web-publishing-personas
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03]
files_modified: []
autonomous: false
requirements: [WEB-01, WEB-02, WEB-03, PERS-01, PERS-02]
must_haves:
  truths:
    - "Full automated test suite passes green with no failures"
    - "HTTP server starts and responds at the /mcp endpoint (WEB-01 verified)"
    - "finance-analyst command produces equity-analyst framing in response (PERS-01 verified)"
    - "finance-pm command produces portfolio-manager framing in response (PERS-02 verified)"
    - "Plugin manifest validated and package structure confirmed complete (WEB-03 verified)"
    - "Human confirms ngrok tunnel produces a reachable public URL (WEB-02 verified)"
  artifacts:
    - path: "src/finance_mcp/server_http.py"
      provides: "HTTP transport entry point"
      contains: "streamable-http"
    - path: ".claude/commands/finance-analyst.md"
      provides: "Equity analyst persona"
      contains: "sell-side equity analyst"
    - path: ".claude/commands/finance-pm.md"
      provides: "Portfolio manager persona"
      contains: "portfolio manager"
    - path: "finance-mcp-plugin/.claude-plugin/plugin.json"
      provides: "Marketplace plugin manifest"
      contains: "finance-mcp"
    - path: "scripts/start_web.sh"
      provides: "Non-technical user launch script"
      contains: "ngrok"
  key_links:
    - from: "tests/test_http_server.py"
      to: "src/finance_mcp/server_http.py"
      via: "import + mocked mcp.run"
      pattern: "streamable-http"
    - from: "tests/test_persona_commands.py"
      to: ".claude/commands/finance-analyst.md"
      via: "file read + frontmatter parse"
      pattern: "sell-side equity analyst"
    - from: "tests/test_plugin_manifest.py"
      to: "finance-mcp-plugin/.claude-plugin/plugin.json"
      via: "json.loads"
      pattern: "finance-mcp"
---

<objective>
Phase 4 functional testing — verify the full automated test suite is green, then perform human verification of the persona commands and HTTP server startup (WEB-01, WEB-02, PERS-01, PERS-02, WEB-03).

Purpose: All five Phase 4 requirements must be confirmed working before Phase 4 is marked complete. Automated tests cover structure and contracts; human verification confirms the persona framing actually works in Claude Code and that the HTTP server can be started.
Output: Phase 4 sign-off
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-web-publishing-personas/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite</name>
  <files></files>
  <action>
    Run the complete test suite and confirm all tests pass:
    ```bash
    cd /Users/aarjay/Documents/machine_learning_skill
    .venv/bin/python -m pytest tests/ -v --tb=short
    ```

    Expected green tests (from prior phases + Phase 4 additions):
    - tests/test_http_server.py — 4 tests (WEB-01)
    - tests/test_persona_commands.py — 9 tests (PERS-01, PERS-02)
    - tests/test_plugin_manifest.py — 6 tests (WEB-03)
    - All prior phase tests (market tools, ML tools, output format, etc.)

    If any test fails: diagnose and fix before proceeding to the checkpoint.
    Do not mark this task complete until ALL tests pass.

    Also verify the HTTP server module can be imported cleanly:
    ```bash
    .venv/bin/python -c "from finance_mcp.server_http import start; print('HTTP server import OK')"
    ```
  </action>
  <verify>
    <automated>cd /Users/aarjay/Documents/machine_learning_skill && .venv/bin/python -m pytest tests/ -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>Full test suite exits 0. No failures or errors. All Phase 4 test files (test_http_server.py, test_persona_commands.py, test_plugin_manifest.py) show green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 4 deliverables — all five requirements:
    - WEB-01: HTTP server entry point (src/finance_mcp/server_http.py)
    - WEB-02: Non-technical startup script (scripts/start_web.sh) with ngrok connection guide
    - WEB-03: Plugin package (finance-mcp-plugin/) ready for marketplace submission
    - PERS-01: /finance-analyst command (.claude/commands/finance-analyst.md)
    - PERS-02: /finance-pm command (.claude/commands/finance-pm.md)
  </what-built>
  <how-to-verify>
    **Verify PERS-01 — Equity Analyst Persona:**
    1. In Claude Code, run: `/finance-analyst compare AAPL vs MSFT for 2024`
    2. Confirm response leads with "From an equity perspective..." framing
    3. Confirm response includes peer comparison and ends with the disclaimer

    **Verify PERS-02 — Portfolio Manager Persona:**
    1. In Claude Code, run: `/finance-pm risk metrics for AAPL MSFT JNJ for 2023`
    2. Confirm response leads with "From a portfolio perspective..." framing
    3. Confirm response emphasizes correlation, drawdown, and beta
    4. Confirm response ends with the disclaimer

    **Verify WEB-01/WEB-02 — HTTP Server Startup (optional, requires ngrok):**
    1. If ngrok is installed: `bash scripts/start_web.sh`
    2. Confirm a public HTTPS URL is printed (or ngrok dashboard shows it at http://localhost:4040)
    3. URL format should be: https://xxxxx.ngrok-free.app/mcp
    4. (Optional) In claude.ai Settings > Connectors, add the URL and confirm connection

    **Verify WEB-03 — Plugin Structure:**
    1. `ls finance-mcp-plugin/` — should show .claude-plugin/, commands/, skills/, .mcp.json
    2. `cat finance-mcp-plugin/.claude-plugin/plugin.json` — confirm valid JSON with name, version, description
    3. `ls finance-mcp-plugin/commands/` — should show finance.md, finance-analyst.md, finance-pm.md
  </how-to-verify>
  <resume-signal>
    Type "approved" if persona commands work correctly and plugin structure looks good.
    Or describe any issues found (wrong framing, missing disclaimer, missing files, etc.)
  </resume-signal>
</task>

</tasks>

<verification>
Phase 4 is complete when:
1. All automated tests pass (test_http_server.py, test_persona_commands.py, test_plugin_manifest.py + all prior)
2. Human verification approved for PERS-01, PERS-02, and WEB-03
3. WEB-02 HTTP startup confirmed (or acknowledged as requiring ngrok install)
</verification>

<success_criteria>
- Full pytest suite green (0 failures)
- /finance-analyst produces equity-analyst framing in Claude Code
- /finance-pm produces portfolio-manager framing in Claude Code
- Plugin package structure is complete and plugin.json is valid
- HTTP server module confirmed importable
- Human verification sign-off received
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-publishing-personas/04-04-SUMMARY.md`
</output>

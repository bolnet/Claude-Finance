---
phase: 04-web-publishing-personas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_persona_commands.py
  - .claude/commands/finance-analyst.md
  - .claude/commands/finance-pm.md
autonomous: true
requirements: [PERS-01, PERS-02]
must_haves:
  truths:
    - "finance-analyst.md exists with equity-analyst role framing and correct frontmatter"
    - "finance-pm.md exists with portfolio-manager role framing and correct frontmatter"
    - "Both persona files include all required allowed-tools matching the base finance.md"
    - "Both persona files contain the hardcoded disclaimer requirement"
    - "Automated unit tests verify frontmatter fields and role framing text for both personas"
  artifacts:
    - path: ".claude/commands/finance-analyst.md"
      provides: "Equity analyst persona command (PERS-01)"
      contains: "sell-side equity analyst"
    - path: ".claude/commands/finance-pm.md"
      provides: "Portfolio manager persona command (PERS-02)"
      contains: "portfolio manager"
    - path: "tests/test_persona_commands.py"
      provides: "Unit tests for both persona command files"
      exports: ["test_analyst_command_exists", "test_analyst_frontmatter", "test_pm_command_exists", "test_pm_frontmatter"]
  key_links:
    - from: ".claude/commands/finance-analyst.md"
      to: "MCP tools"
      via: "allowed-tools frontmatter including mcp__finance__* tools"
      pattern: "mcp__finance__"
    - from: ".claude/commands/finance-pm.md"
      to: "MCP tools"
      via: "allowed-tools frontmatter including mcp__finance__* tools"
      pattern: "mcp__finance__"
---

<objective>
Create two persona command variants for Claude Code: finance-analyst (equity-focused) and finance-pm (portfolio manager-focused) — PERS-01 and PERS-02.

Purpose: Finance professionals have different analytical lenses. An equity analyst focuses on single-stock and peer comparison; a portfolio manager focuses on portfolio-level risk/return. Separate command files give each persona tailored framing with zero new Python code — all existing MCP tools are reused.
Output: .claude/commands/finance-analyst.md, .claude/commands/finance-pm.md, tests/test_persona_commands.py
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-web-publishing-personas/04-RESEARCH.md

<interfaces>
<!-- Key types and patterns the executor needs. Extracted from codebase. -->

From .claude/commands/finance.md (the base command to model both personas on):
```markdown
---
description: Run financial analysis from plain English — stock prices, returns, risk metrics, ML models
argument-hint: "[analysis request, e.g. 'show AAPL price chart for 2023']"
allowed-tools: Bash(python3:*), Bash(pip:*), Write, Read, mcp__finance__ping, mcp__finance__validate_environment
model: sonnet
---
```

From 04-RESEARCH.md Pattern 3 (Persona Command Variants):

finance-analyst.md role framing:
- description: "Equity analyst view — stock analysis, peer comparison, and valuation metrics"
- argument-hint: "[e.g. 'compare AAPL vs MSFT vs GOOGL for 2024']"
- Role: "You are a sell-side equity analyst."
- Emphasis: peer comparison, relative returns, Sharpe/drawdown vs sector peers
- Output framing: "From an equity perspective..."
- Disclaimer: mandatory on every response

finance-pm.md role framing:
- description: "Portfolio manager view — risk/return attribution, portfolio-level metrics"
- argument-hint: "[e.g. 'risk metrics for a portfolio of AAPL MSFT JNJ for 2023']"
- Role: "You are a portfolio manager."
- Emphasis: correlation heatmaps, portfolio volatility, drawdown and beta as primary risk metrics
- Output framing: "From a portfolio perspective..."
- Disclaimer: mandatory on every response

CRITICAL anti-pattern: Do NOT use `!command` dynamic context injection in persona command files.
The `!` syntax only works in Claude Code terminal context. Keep it only in the base finance.md.
However, these persona files ARE Claude Code commands (not claude.ai web commands), so
the `!` injection CAN be used here since they only run in Claude Code. Include it to match
the base finance.md pattern for environment context.

Actually, per the research: persona command files work for Claude Code; the anti-pattern
applies only when planning for claude.ai. Since these are .claude/commands/ files for
Claude Code, include the same !-injection context block as finance.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test stubs for persona commands (Wave 0)</name>
  <files>tests/test_persona_commands.py</files>
  <behavior>
    - test_analyst_command_exists: .claude/commands/finance-analyst.md exists
    - test_analyst_frontmatter_fields: file has YAML frontmatter with description, argument-hint, allowed-tools, model fields
    - test_analyst_role_framing: file body contains "sell-side equity analyst" and "equity perspective"
    - test_analyst_disclaimer_required: file body contains "disclaimer" (case-insensitive)
    - test_pm_command_exists: .claude/commands/finance-pm.md exists
    - test_pm_frontmatter_fields: file has YAML frontmatter with description, argument-hint, allowed-tools, model fields
    - test_pm_role_framing: file body contains "portfolio manager" and "portfolio perspective"
    - test_pm_disclaimer_required: file body contains "disclaimer" (case-insensitive)
    - test_both_commands_include_mcp_tools: both files reference mcp__finance__ in allowed-tools
  </behavior>
  <action>
    Create tests/test_persona_commands.py. Read each .md file as text, parse the YAML frontmatter
    block (between first --- and second ---), and check body text.

    Helper pattern for reading frontmatter:
    ```python
    from pathlib import Path
    import re

    def read_command_file(name):
        path = Path(__file__).parent.parent / ".claude" / "commands" / name
        text = path.read_text()
        # Split on --- delimiter
        parts = re.split(r'^---\s*$', text, flags=re.MULTILINE)
        # parts[0] is empty, parts[1] is frontmatter YAML, parts[2] is body
        return parts[1] if len(parts) > 1 else "", parts[2] if len(parts) > 2 else ""

    import yaml  # stdlib not available; use simple key: value parsing
    # OR just check with regex: assert "description:" in frontmatter
    ```

    Do NOT import yaml (not in dependencies). Use string/regex checks on the raw frontmatter text.

    All nine stubs must fail (RED) — the .md files do not exist yet.
  </action>
  <verify>
    <automated>cd /Users/aarjay/Documents/machine_learning_skill && .venv/bin/python -m pytest tests/test_persona_commands.py -x -q --tb=short 2>&1 | head -20</automated>
  </verify>
  <done>Nine test stubs exist and fail with FileNotFoundError or AssertionError. Tests are RED before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Create finance-analyst and finance-pm persona command files</name>
  <files>.claude/commands/finance-analyst.md, .claude/commands/finance-pm.md</files>
  <action>
    Create .claude/commands/finance-analyst.md:

    ```markdown
    ---
    description: Equity analyst view — stock analysis, peer comparison, and valuation metrics
    argument-hint: "[e.g. 'compare AAPL vs MSFT vs GOOGL for 2024']"
    allowed-tools: Bash(python3:*), Bash(pip:*), Write, Read, mcp__finance__ping, mcp__finance__validate_environment, mcp__finance__analyze_stock, mcp__finance__compare_tickers, mcp__finance__get_returns, mcp__finance__get_volatility, mcp__finance__get_risk_metrics, mcp__finance__correlation_map
    model: sonnet
    ---

    Equity analysis request: $ARGUMENTS

    ## Environment Context

    > Injected before code generation to confirm the environment is ready.

    Python version: !`python3 --version 2>&1`
    Finance packages: !`python3 -c "import yfinance, pandas, numpy, matplotlib, seaborn, sklearn; print('All OK')" 2>&1`
    Working directory: !`pwd`

    ## Instructions

    You are a **sell-side equity analyst**. Your default lens is single-stock and peer-group analysis.

    ### Role Framing

    Lead every analysis with equity-analyst framing: "From an equity perspective..."

    ### Intent Routing

    | User says... | Intent | Action |
    |---|---|---|
    | "compare", "vs", "peer", "sector" | peer-comparison | Use `compare_tickers` + `correlation_map`; normalize prices to 100 at start date |
    | "price", "chart", "performance", "show me [TICKER]" | stock-analysis | Use `analyze_stock`; follow with return vs benchmark commentary |
    | "risk", "Sharpe", "drawdown", "beta" | risk-analysis | Use `get_risk_metrics`; compare Sharpe/drawdown vs S&P 500 benchmark |
    | "returns", "gains", "cumulative" | returns-analysis | Use `get_returns` + `get_volatility`; annualize volatility |
    | "correlation", "heatmap", "co-movement" | correlation | Use `correlation_map`; explain most/least correlated pairs |
    | "environment", "packages", "installed" | environment-check | Call `validate_environment` and report |
    | "ping", "status", "running" | health-check | Call `ping` |

    ### Emphasis

    1. **Peer comparison first**: When analyzing any ticker, offer to add sector peers for context.
    2. **Relative performance**: Express returns relative to S&P 500 benchmark ("AAPL +18% vs S&P +14%").
    3. **Risk-adjusted framing**: Prefer Sharpe ratio and drawdown over raw returns when discussing performance quality.
    4. **Valuation context**: When discussing price trends, note any available context on price levels relative to history.

    ### Output Rules

    - Lead with plain-English interpretation before any numbers or charts
    - Chart paths are relative to `finance_output/charts/`
    - Every response ends with the mandatory disclaimer:
      > **Disclaimer:** For educational and informational purposes only. Not financial advice. Past performance does not guarantee future results.
    ```

    ---

    Create .claude/commands/finance-pm.md:

    ```markdown
    ---
    description: Portfolio manager view — risk/return attribution, portfolio-level metrics
    argument-hint: "[e.g. 'risk metrics for a portfolio of AAPL MSFT JNJ for 2023']"
    allowed-tools: Bash(python3:*), Bash(pip:*), Write, Read, mcp__finance__ping, mcp__finance__validate_environment, mcp__finance__get_risk_metrics, mcp__finance__compare_tickers, mcp__finance__correlation_map, mcp__finance__get_volatility, mcp__finance__get_returns, mcp__finance__analyze_stock
    model: sonnet
    ---

    Portfolio analysis request: $ARGUMENTS

    ## Environment Context

    > Injected before code generation to confirm the environment is ready.

    Python version: !`python3 --version 2>&1`
    Finance packages: !`python3 -c "import yfinance, pandas, numpy, matplotlib, seaborn, sklearn; print('All OK')" 2>&1`
    Working directory: !`pwd`

    ## Instructions

    You are a **portfolio manager**. Your default lens is portfolio-level attribution and risk management.

    ### Role Framing

    Lead every analysis with portfolio framing: "From a portfolio perspective..."

    ### Intent Routing

    | User says... | Intent | Action |
    |---|---|---|
    | "portfolio", "holdings", "book", "positions" | portfolio-overview | Use `compare_tickers` (normalized) + `correlation_map`; treat tickers as holdings |
    | "risk", "drawdown", "beta", "Sharpe" | risk-attribution | Use `get_risk_metrics` for each holding; summarize portfolio-level implications |
    | "correlation", "diversification", "co-movement" | diversification | Use `correlation_map`; interpret diversification benefit or concentration risk |
    | "volatility", "vol", "standard deviation" | volatility-analysis | Use `get_volatility` per holding; aggregate to portfolio-level commentary |
    | "returns", "attribution", "performance" | return-attribution | Use `get_returns` per holding; attribute contribution to portfolio total |
    | "price", "chart", "show me [TICKER]" | individual-analysis | Use `analyze_stock`; frame in context of its portfolio role |
    | "environment", "packages", "installed" | environment-check | Call `validate_environment` and report |
    | "ping", "status", "running" | health-check | Call `ping` |

    ### Emphasis

    1. **Portfolio-level aggregation**: Treat the set of tickers as holdings in a book, not isolated securities.
    2. **Correlation and diversification**: Always run `correlation_map` for multi-ticker requests; flag concentration risk if high correlation.
    3. **Drawdown and beta as primary risk metrics**: Report max drawdown and beta vs S&P 500 before Sharpe ratio.
    4. **Portfolio volatility context**: Compare individual holding volatility to portfolio-level volatility impact.

    ### Output Rules

    - Lead with plain-English interpretation before any numbers or charts
    - Chart paths are relative to `finance_output/charts/`
    - Every response ends with the mandatory disclaimer:
      > **Disclaimer:** For educational and informational purposes only. Not financial advice. Past performance does not guarantee future results.
    ```

    After writing both files, run the full test suite. All nine tests in test_persona_commands.py must pass.
  </action>
  <verify>
    <automated>cd /Users/aarjay/Documents/machine_learning_skill && .venv/bin/python -m pytest tests/test_persona_commands.py -v --tb=short</automated>
  </verify>
  <done>Both .claude/commands/finance-analyst.md and .claude/commands/finance-pm.md exist. All nine tests in test_persona_commands.py pass. Full test suite green.</done>
</task>

</tasks>

<verification>
Full suite check after both tasks complete:
```bash
cd /Users/aarjay/Documents/machine_learning_skill && .venv/bin/python -m pytest tests/ -v --tb=short
```

Verify files exist:
```bash
ls /Users/aarjay/Documents/machine_learning_skill/.claude/commands/
# Should show: finance.md  finance-analyst.md  finance-pm.md
```
</verification>

<success_criteria>
- .claude/commands/finance-analyst.md exists with sell-side equity analyst framing (PERS-01)
- .claude/commands/finance-pm.md exists with portfolio manager framing (PERS-02)
- Both files have correct frontmatter (description, argument-hint, allowed-tools, model)
- Both files include mandatory disclaimer requirement
- Both files include intent routing table referencing existing MCP tools
- All 9 tests in test_persona_commands.py pass
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/04-web-publishing-personas/04-02-SUMMARY.md`
</output>

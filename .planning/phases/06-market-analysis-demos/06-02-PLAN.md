---
phase: 06-market-analysis-demos
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - tests/test_market_demo.py
autonomous: false
requirements:
  - MRKT-01
  - MRKT-02
  - MRKT-03
  - MRKT-04
  - MRKT-05
  - MRKT-06

must_haves:
  truths:
    - "User sees analyze_stock produce a price chart for AAPL with plain-English explanation"
    - "User sees get_returns output with daily and cumulative return values explained"
    - "User sees get_volatility output with annualized volatility explained"
    - "User sees get_risk_metrics output with Sharpe ratio, max drawdown, and beta explained"
    - "User sees compare_tickers chart for AAPL vs MSFT with explanation"
    - "User sees correlation_map heatmap for AAPL/MSFT/GOOGL with explanation"
  artifacts:
    - path: "tests/test_market_demo.py"
      provides: "Live integration tests (from 06-01) all passing"
  key_links:
    - from: ".claude/commands/demo.md"
      to: "mcp__finance__analyze_stock"
      via: "MCP tool call during demo execution"
      pattern: "analyze_stock"
    - from: ".claude/commands/demo.md"
      to: "mcp__finance__get_risk_metrics"
      via: "MCP tool call during demo execution"
      pattern: "get_risk_metrics"
---

<objective>
Run the 6 market analysis demo steps (Steps 3-8 from demo.md) end-to-end and verify each tool produces correct output with plain-English explanations. Human confirms all 6 tools work as expected.

Purpose: Automated tests from 06-01 prove the tools work at the function level. This plan verifies the full demo experience — that the MCP tool calls from demo.md produce the right output and that the plain-English explanations match the data.

Output: Human-verified confirmation that all 6 market analysis demo steps work correctly
</objective>

<execution_context>
@/Users/aarjay/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aarjay/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-market-analysis-demos/06-01-SUMMARY.md
@.claude/commands/demo.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full integration test suite to confirm green baseline</name>
  <files>tests/test_market_demo.py</files>
  <action>
Run the full test suite to confirm all existing tests plus the 6 new market demo integration tests pass.

Execute:
```bash
.venv/bin/python3 -m pytest tests/ -v --tb=short 2>&1
```

If any market demo tests fail due to transient network issues (Yahoo Finance timeouts), retry once. If they fail due to code issues, investigate and fix in test_market_demo.py only (do not modify tool source code).

Report the full test count: X passed, Y xpassed, Z failed.
  </action>
  <verify>
    <automated>.venv/bin/python3 -m pytest tests/ -v --tb=short 2>&1 | tail -5</automated>
  </verify>
  <done>Full test suite passes with zero failures; all 6 market demo integration tests are green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of market analysis demo steps</name>
  <files></files>
  <action>Present the human verification checkpoint for demo Steps 3-8.</action>
  <what-built>All 6 market analysis MCP tools verified via live integration tests. The demo steps (3-8) are ready for end-to-end execution. Each tool has been proven to: (1) fetch real data from Yahoo Finance, (2) compute correct metrics/charts, (3) return formatted output with plain-English explanations.</what-built>
  <how-to-verify>
    1. In Claude Code, type `/demo` to start the demo walkthrough
    2. Let the demo run through Steps 1-2 (environment check, ping) -- these are from Phase 5
    3. Watch Steps 3-8 carefully (market analysis tools):
       - Step 3 (analyze_stock): Confirm a price chart PNG is produced for AAPL and an explanation of the price trend is shown
       - Step 4 (get_returns): Confirm daily/cumulative return values are shown with an explanation
       - Step 5 (get_volatility): Confirm annualized volatility value is shown with an explanation of what it means
       - Step 6 (get_risk_metrics): Confirm Sharpe ratio, max drawdown, and beta values are shown with explanations of each
       - Step 7 (compare_tickers): Confirm a normalized comparison chart for AAPL vs MSFT is produced with explanation
       - Step 8 (correlation_map): Confirm a correlation heatmap for AAPL/MSFT/GOOGL is produced with explanation
    4. Steps 9-11 will be skipped (sample CSV not yet available) -- this is expected
    5. Verify each step's explanation is in plain English (not raw numbers/code)
    6. Verify chart files are saved to finance_output/charts/
  </how-to-verify>
  <verify>Human approves all 6 market analysis demo steps</verify>
  <done>Human confirms MRKT-01 through MRKT-06 all working correctly in live demo</done>
  <resume-signal>Type "approved" if all 6 market analysis steps work correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- Full test suite green (zero failures)
- Human confirms all 6 market analysis demo steps produce correct output
- Each step includes plain-English explanation
- Chart files are generated in finance_output/charts/
</verification>

<success_criteria>
- All 6 market analysis tools (MRKT-01 through MRKT-06) demonstrated live with real ticker data
- Human sign-off confirms demo Steps 3-8 work end-to-end
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/06-market-analysis-demos/06-02-SUMMARY.md`
</output>
